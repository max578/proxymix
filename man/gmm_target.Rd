% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/proxymix-classes.R
\name{gmm_target}
\alias{gmm_target}
\title{A target density on R^p}
\usage{
gmm_target(
  n_dim = integer(0),
  log_density = NULL,
  samples = NULL,
  support = NULL,
  normalised = NA,
  log_normalizer = NA_real_,
  name = "gmm_target",
  metadata = list()
)
}
\arguments{
\item{n_dim}{Integer scalar — the ambient dimension \code{p} (the property is
called \code{n_dim} rather than \code{dim} because S7 reserves \code{dim} as an
attribute name).}

\item{log_density}{Optional function: \verb{function(x)} taking a numeric matrix
\code{n} by \code{p} and returning a length-\code{n} numeric vector of \verb{log f(x)}.}

\item{samples}{Optional \code{n} by \code{p} numeric matrix of i.i.d. samples
from the target.}

\item{support}{Optional declaration of the target's support. \code{NULL} (the
default) means the full \eqn{\mathbb{R}^p}. Otherwise a list
\code{list(lower = , upper = )} of per-coordinate bounds, each of length 1
(recycled) or \code{n_dim}, with \code{-Inf} / \code{Inf} permitted for unbounded
coordinates. A bounded or one-sided support drives automatic
support-matched importance proposal selection in regime (iii); see
\code{\link[=fit_kld_em]{fit_kld_em()}}.}

\item{normalised}{Logical scalar declaring whether \code{log_density}
integrates to one. \code{TRUE}, \code{FALSE}, or \code{NA} (unknown). Defaults to
\code{NA}. Downstream diagnostics treat \code{NA} and \code{FALSE} identically and
label any KLD estimate as shifted.}

\item{log_normalizer}{Numeric scalar \verb{log Z(f)} of the supplied
\code{log_density}, if known. Default \code{NA_real_}. When \code{normalised = FALSE}
and \code{log_normalizer} is finite, downstream diagnostics can correct
shifted KLD estimates by \code{+ log_normalizer}.}

\item{name}{Human-readable name.}

\item{metadata}{Optional list of additional descriptors.}
}
\value{
An S7 object of class \code{gmm_target}.
}
\description{
An S7 representation of a target density that \code{proxymix} is asked to
approximate. A target may carry an evaluable \code{log_density}, a matrix of
i.i.d. \code{samples}, or both. Each of the three fitting regimes consumes a
different subset:
}
\details{
\itemize{
\item regime \code{"moment"} needs samples (or both moments via metadata);
\item regime \code{"sample"} needs samples;
\item regime \code{"kld"} needs the log-density.
}

Use \code{\link[=gmm_target]{gmm_target()}} or \code{\link[=gmm_target_from_samples]{gmm_target_from_samples()}} to construct.

Importance-sampled KLD-EM (regime \code{"kld"}) only requires \code{log_density}
to be specified up to an unknown additive constant — the self-normalised
weights are invariant to scaling. The package's \emph{diagnostics} downstream,
however, do depend on normalisation: an importance-sampled KLD estimate
against an unnormalised log-density measures
\eqn{\widehat{KL}(f \Vert g) - \log Z(f)} rather than
\eqn{\widehat{KL}(f \Vert g)}, and a squared-Hellinger Monte Carlo
estimate is only meaningful when both densities integrate to one.
Declare the target's normalisation explicitly via \code{normalised} (and,
where possible, supply \code{log_normalizer}) so that the package can label
shifted KLDs as shifted and refuse misleading Hellinger reports.
}
\examples{
tgt <- banana_target()
tgt
}
\seealso{
Other classes: 
\code{\link{autoplot.gmm_fit}},
\code{\link{gmm}()},
\code{\link{gmm_counterfactual_law}()},
\code{\link{gmm_dim}()},
\code{\link{gmm_fit}()},
\code{\link{gmm_n_components}()},
\code{\link{is_proposal}()}
}
\concept{classes}
