% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/from_posterior.R
\name{gmm_target_from_posterior}
\alias{gmm_target_from_posterior}
\alias{gmm_target_from_posterior.default}
\alias{gmm_target_from_posterior.function}
\title{Compile an unnormalised Bayesian posterior into a \code{gmm_target}}
\usage{
gmm_target_from_posterior(model, ...)

\method{gmm_target_from_posterior}{default}(model, ...)

\method{gmm_target_from_posterior}{`function`}(
  model,
  ...,
  parameter_names = NULL,
  log_normalizer = NA_real_,
  name = NULL
)
}
\arguments{
\item{model}{One of:
\itemize{
\item a function — a bare callable satisfying the contract above;
\item a fitted model object whose class registers a
\verb{gmm_target_from_posterior.<class>} method in its own package.
}}

\item{...}{Forwarded to method-specific implementations.}

\item{parameter_names}{Character vector of parameter names. Required
for the \code{function} method (or attached as
\code{attr(model, "parameter_names")}). The length determines \code{n_dim}.}

\item{log_normalizer}{Numeric scalar \verb{log Z} of the posterior, if known.
\code{NA_real_} (the default) otherwise; downstream diagnostics will label
any KLD estimate as shifted.}

\item{name}{Optional human-readable target name. Defaults to
\code{"posterior"}.}
}
\value{
A \link{gmm_target} with \code{normalised = FALSE} and the user-supplied
\code{log_normalizer} (or \code{NA_real_}).
}
\description{
Generic S3 constructor that turns a Bayesian posterior — represented
either by a fitted model object (e.g. from \code{brms} or \code{Stan}) or by a
bare callable — into a \code{gmm_target} suitable for regime (iii) of
\code{\link[=fit_proxymix]{fit_proxymix()}} / \code{\link[=fit_kld_em]{fit_kld_em()}}.
}
\details{
The contract for the underlying callable is:
\itemize{
\item \strong{Vectorised}: accepts a numeric matrix with rows indexing
independent parameter draws and columns indexing parameters; returns
a length-\code{nrow(theta)} numeric vector of \verb{log p(theta | data) + const}.
\item \strong{Unnormalised is fine}: the marginal likelihood \verb{log Z} is not
required. Where the source package can supply it, pass
\code{log_normalizer}.
\item \strong{Side-effect free}: no plotting, no mutable state. Pure function.
\item \strong{Domain-safe}: returns \code{-Inf} outside support rather than raising
an error.
}

The default method errors with a hint pointing the user at either
(a) a Bayesian package that registers a method, or (b) the
\code{function} method below.
}
\examples{
# A trivial unnormalised log-posterior: a 2D banana centred near (1, 0).
log_post <- function(theta) {
  x <- theta[, 1L]
  y <- theta[, 2L]
  -0.5 * (x^2 + (y - 0.1 * x^2 + 1)^2)
}
tgt <- gmm_target_from_posterior(
  log_post,
  parameter_names = c("x", "y")
)
tgt
}
\concept{interop}
