% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gmm_target_constructors.R
\name{maxent_target}
\alias{maxent_target}
\title{Maximum-entropy target under moment and support constraints}
\usage{
maxent_target(moments = NULL, support = NULL, name = "maxent_target")
}
\arguments{
\item{moments}{Either \code{NULL} (only a support constraint, giving the uniform)
or a list with \code{mean} (length-\code{p} numeric) and \code{cov} (a \code{p}-by-\code{p}
symmetric positive-definite matrix). Supplying \code{mean} without \code{cov} (or vice
versa) is an error.}

\item{support}{\code{NULL} for the full \eqn{\mathbb{R}^p} (only valid with
second-moment \code{moments}, giving the Gaussian), or a list
\code{list(lower = , upper = )} of per-coordinate box bounds (each length 1,
recycled, or length \code{p}). The uniform case requires finite bounds.}

\item{name}{Human-readable name.}
}
\value{
A \link{gmm_target}.
}
\description{
Builds the maximum-entropy \link{gmm_target} consistent with the supplied
constraints, the least-committal density given what is known. The maximum-
entropy density under linear constraints is an exponential family
\eqn{p(x) \propto \exp(\eta^\top T(x))} on the support, and three cases admit
an exact closed form:
}
\details{
\itemize{
\item \strong{First- and second-moment constraints on full support} -- the Gaussian
\eqn{\mathcal{N}(\mathrm{mean}, \mathrm{cov})}. The moment constraints are
realised exactly, so a regime-(i) moment match recovers the target, and the
target carries its \code{moments} in \code{metadata} for that purpose.
\item \strong{First- and second-moment constraints on a box support} -- the same
canonical Gaussian form restricted to the box, a \emph{truncated Gaussian}. The
normaliser is closed-form when \code{cov} is diagonal (a product of univariate
Gaussian box probabilities) and the target is then exactly normalised;
otherwise the target is declared unnormalised and regime (iii) fits it up to
the unknown constant. Truncation shifts the realised moments inward, so the
truncated density's mean and covariance are not \code{mean} and \code{cov}; it is the
canonical-form maximum-entropy density on the box, not the moment-matched
one.
\item \strong{A support constraint alone (no moments) on a finite box} -- the uniform
density, the maximum-entropy density on a compact support. Its differential
entropy is exactly \eqn{\log \mathrm{vol}(\mathrm{box})}, the largest
attainable on that support.
}

The bounded-support cases declare their \code{support}, so \code{\link[=fit_kld_em]{fit_kld_em()}} (and
\code{\link[=fit_proxymix]{fit_proxymix()}} with \code{regime = "kld"}) selects a support-matched uniform
importance proposal automatically. Together with the Gaussian as the least-
committal full-support density and the \code{\link[=epanechnikov_target]{epanechnikov_target()}} as a compact-
support obstruction, these complete a family of principled test targets.
}
\examples{
## Full-support second-moment maximum entropy is the Gaussian.
g <- maxent_target(moments = list(mean = c(0, 0), cov = diag(2)))
g@log_density(matrix(c(0, 0), nrow = 1L))

## Support alone on a box is the uniform.
u <- maxent_target(support = list(lower = 0, upper = 1))
exp(u@log_density(matrix(c(0.5), nrow = 1L)))

## Second moments on a box is a truncated Gaussian, fit via regime (iii).
tg <- maxent_target(moments = list(mean = 0, cov = matrix(1)),
                    support = list(lower = -2, upper = 2))
tg
}
\references{
Jaynes, E. T. (1957) Information theory and statistical mechanics.
\emph{Physical Review} 106(4), 620--630. \doi{10.1103/PhysRev.106.620}
}
\seealso{
Other targets: 
\code{\link{banana_target}()},
\code{\link{donut_target}()},
\code{\link{epanechnikov_target}()},
\code{\link{gmm_target_from_samples}()},
\code{\link{mixture_target}()}
}
\concept{targets}
