% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/algebra.R
\name{gmm_product}
\alias{gmm_product}
\title{Pointwise product of two Gaussian mixtures}
\usage{
gmm_product(g1, g2, reduce = NULL)
}
\arguments{
\item{g1, g2}{Two \link{gmm} (or \link{gmm_fit}) objects of the same ambient
dimension.}

\item{reduce}{Optional positive integer: reduce the product to at most
this many components via \code{\link[=gmm_reduce]{gmm_reduce()}}. Default \code{NULL} (no
reduction).}
}
\value{
A \link{gmm} with \code{K1 * K2} components (or at most \code{reduce}), with
\code{metadata$log_integral} recording \eqn{\log \int p\, q}.
}
\description{
The normalised pointwise product of two mixture densities -- the
conjugate Bayes update when one mixture plays the prior and the other
the likelihood. The product of two Gaussian mixtures is again a
Gaussian mixture with \eqn{K_1 K_2} components:
\deqn{p(x)\, q(x) \propto \sum_{ij} w_i v_j z_{ij}\,
  \mathcal{N}\!\left(x;\ \mu_{ij},\ \Sigma_{ij}\right),}
with \eqn{\Sigma_{ij} = (\Sigma_i^{-1} + S_j^{-1})^{-1}},
\eqn{\mu_{ij} = \Sigma_{ij} (\Sigma_i^{-1} \mu_i + S_j^{-1} m_j)}, and
\eqn{z_{ij} = \mathcal{N}(\mu_i;\ m_j,\ \Sigma_i + S_j)} evaluated in
the log domain. The overall normaliser
\eqn{\int p\, q = \sum_{ij} w_i v_j z_{ij}} is returned in the result's
metadata as \code{log_integral} (it is the marginal evidence of the update).
}
\details{
Because the component count multiplies, chained products grow
geometrically; pass \code{reduce} to cap the count via \code{\link[=gmm_reduce]{gmm_reduce()}} after
the product (the standard assumed-density trick), or manage the budget
yourself.
}
\examples{
prior <- gmm(weights = c(0.5, 0.5), means = list(-2, 2),
             covariances = list(matrix(1), matrix(1)))
lik <- gmm(weights = 1, means = list(0.5), covariances = list(matrix(0.5)))
post <- gmm_product(prior, lik)
post@weights
}
\seealso{
Other operators: 
\code{\link{gmm_affine}()},
\code{\link{gmm_aggregate}()},
\code{\link{gmm_convolve}()},
\code{\link{gmm_counterfactual}()},
\code{\link{gmm_filter}()},
\code{\link{gmm_intervene}()},
\code{\link{gmm_missing}()},
\code{\link{gmm_mix}()},
\code{\link{gmm_observe}()},
\code{\link{gmm_reduce}()}
}
\concept{operators}
