% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/init.R
\name{multi_start_best_of}
\alias{multi_start_best_of}
\title{Multi-start best-of wrapper}
\usage{
multi_start_best_of(fit_fn, inits, score_fn, ...)
}
\arguments{
\item{fit_fn}{A function with signature \verb{function(init, ...)} returning
a \link{gmm_fit}.}

\item{inits}{A list of \link{gmm} initialisations.}

\item{score_fn}{A function \verb{function(fit)} returning a numeric score —
\emph{larger is better} (typically the final log-target evidence).}

\item{...}{Additional arguments forwarded to \code{fit_fn}.}
}
\value{
The \link{gmm_fit} with the largest \code{score_fn(fit)}.
}
\description{
Runs the supplied fitter from each of several initialisations and
returns the fit with the best score, following Karlis and Xekalaki
(2003)'s recommendation.
}
\examples{
x <- matrix(stats::rnorm(200), ncol = 2)
tgt <- gmm_target_from_samples(x)
inits <- list(init_random(2L, 2L, seed = 1L),
              init_moment_seed(x, N = 2L))
best <- multi_start_best_of(
  fit_fn   = function(init, ...) fit_em_samples(tgt, init = init, ...),
  inits    = inits,
  score_fn = function(fit) fit@diagnostics$loglik_final,
  max_iter = 25L
)
best@diagnostics$loglik_final
}
\seealso{
Other init: 
\code{\link{init_kmeans}()},
\code{\link{init_moment_seed}()},
\code{\link{init_random}()},
\code{\link{init_warm_start}()}
}
\concept{init}
