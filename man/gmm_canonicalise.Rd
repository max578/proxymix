% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gmm_ops.R
\name{gmm_canonicalise}
\alias{gmm_canonicalise}
\title{Canonicalise the component ordering of a Gaussian mixture}
\usage{
gmm_canonicalise(g)
}
\arguments{
\item{g}{A \link{gmm} (or \link{gmm_fit}) object.}
}
\value{
A \link{gmm} (or \link{gmm_fit}) of the same subclass as \code{g}, with the
components permuted into canonical order.
}
\description{
Returns a new \link{gmm} (or \link{gmm_fit}) with the components permuted into a
canonical order: weight descending, then \verb{||mu||} descending as a
tiebreaker. The mixture distribution is unchanged — only the bookkeeping
order is — but the canonical ordering removes the EM label-switching
nuisance from snapshot tests, cross-run comparisons, and printed
summaries.
}
\details{
Applied automatically by the regime-specific fitters
(\code{\link[=fit_moment_match]{fit_moment_match()}}, \code{\link[=fit_em_samples]{fit_em_samples()}}, \code{\link[=fit_kld_em]{fit_kld_em()}}) and by the
top-level dispatcher \code{\link[=fit_proxymix]{fit_proxymix()}} when \code{canonicalise = TRUE}
(the default).
}
\examples{
g <- gmm(weights = c(0.1, 0.6, 0.3),
         means = list(c(0, 0), c(3, 0), c(-1, 1)),
         covariances = list(diag(2), diag(2), diag(2)))
gmm_canonicalise(g)
}
\seealso{
Other ops: 
\code{\link{dgmm}()},
\code{\link{gmm_conditionalise}()},
\code{\link{gmm_divergence}()},
\code{\link{gmm_kld}()},
\code{\link{gmm_marginalise}()},
\code{\link{rgmm}()}
}
\concept{ops}
