% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decide.R
\name{proxy_decide}
\alias{proxy_decide}
\title{Optimal action and expected incremental value per unit}
\usage{
proxy_decide(
  model,
  newdata,
  value,
  cost = 0,
  t1 = NULL,
  t0 = NULL,
  se_method = c("delta", "mc"),
  ...
)
}
\arguments{
\item{model}{An \link{uplift_model}.}

\item{newdata}{A data frame carrying the covariate columns.}

\item{value}{Numeric scalar -- the value of one unit of outcome.}

\item{cost}{Numeric scalar -- the cost of treating one unit. Default \code{0}.}

\item{t1, t0}{The treated and control treatment values. Default the treatment levels observed at fit time (\code{model@treatment_levels}); a value matching neither observed level aborts.}

\item{se_method}{One of \code{"delta"} (default) or \code{"mc"}.}

\item{...}{Forwarded to \code{\link[=proxy_cate]{proxy_cate()}}.}
}
\value{
A \link[data.table:data.table]{data.table::data.table} with columns \code{id}, \code{action},
\code{expected_value}, \code{tau}, \code{se}, \code{flip_prob}, \code{overlap_flag}.
}
\description{
Turns the per-unit treatment effect into a next-best action under a linear
value model: treat when the value of the effect exceeds the cost, i.e.
\eqn{d^*(x) = 1\{\,\text{value} \cdot \tau(x) > \text{cost}\,\}}, with
expected incremental value \eqn{\text{value} \cdot \tau(x) - \text{cost}}.
The standard error of \code{tau} is propagated to an action-flip probability --
the chance the recommended action would reverse under sampling noise.
}
\examples{
set.seed(1)
n <- 400L
x <- stats::rnorm(n)
t <- stats::rbinom(n, 1L, 0.5)
y <- 1 + (x > 0) * t + stats::rnorm(n, sd = 0.5)
dat <- data.frame(y = y, t = t, x = x)
m <- fit_uplift(dat, "y", "t", "x", N = 2L, regime = "sample",
                max_iter = 50L, seed = 1L)
proxy_decide(m, data.frame(x = c(-1, 1)), value = 1, cost = 0.2)
}
\seealso{
\code{\link[=proxy_cate]{proxy_cate()}}, \code{\link[=proxy_policy_value]{proxy_policy_value()}}

Other decision: 
\code{\link{fit_uplift}()},
\code{\link{gmm_cf_mean}()},
\code{\link{gmm_cf_tail_prob}()},
\code{\link{gmm_cf_variance}()},
\code{\link{gmm_counterfactual}()},
\code{\link{gmm_intervene}()},
\code{\link{proxy_cate}()},
\code{\link{proxy_confounding_gap}()},
\code{\link{proxy_identification_report}()},
\code{\link{proxy_overlap}()},
\code{\link{proxy_policy_value}()},
\code{\link{proxy_predict}()},
\code{\link{proxy_regime_segments}()},
\code{\link{proxy_retrospective_uplift}()},
\code{\link{proxy_uplift}()},
\code{\link{uplift_identification}()},
\code{\link{uplift_model}()}
}
\concept{decision}
