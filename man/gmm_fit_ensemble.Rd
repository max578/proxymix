% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ensemble.R
\name{gmm_fit_ensemble}
\alias{gmm_fit_ensemble}
\title{Bootstrap ensemble of a fitted proxy}
\usage{
gmm_fit_ensemble(fit, B = 200L, max_iter = 25L, tol = 1e-05, seed = NULL)
}
\arguments{
\item{fit}{A \link{gmm_fit} whose fitting inputs are recoverable (a
regime-\code{"kld"} fit carries its importance draws; regimes \code{"sample"}
and \code{"moment"} require the target to carry its samples).}

\item{B}{Number of bootstrap replicates.}

\item{max_iter, tol}{Convergence controls for the per-replicate
warm-started weighted EM.}

\item{seed}{Optional integer seed for the replicate weights.}
}
\value{
A list of class \code{gmm_ensemble}: \code{fit} (the base fit),
\code{members} (a length-\code{B} list of \link{gmm}), \code{B}, and \code{regime}.
}
\description{
Quantifies the sampling variability of the fitted mixture itself by a
Bayesian (weighted) bootstrap: each replicate re-weights the fit's own
observations with Dirichlet(1, ..., 1) weights and refits by a
warm-started weighted EM. In regime \code{"kld"} the observations are the
fit's cached importance draws with their self-normalised weights, so a
replicate costs \strong{zero new target evaluations}; in regimes \code{"sample"}
and \code{"moment"} the observations are the target's samples. Summaries of
any functional of the proxy are then read off the ensemble with
\code{\link[=proxy_functional_ci]{proxy_functional_ci()}} -- functional-space intervals sidestep the
label-switching that makes parameter-space intervals incoherent.
}
\examples{
fit <- fit_proxymix(banana_target(), N = 2L, regime = "kld",
                    is_size = 1500L, max_iter = 20L, seed = 1L)
ens <- gmm_fit_ensemble(fit, B = 30L, seed = 2L)
proxy_functional_ci(ens, gmm_mean)
}
\references{
Rubin, D. B. (1981) The Bayesian bootstrap. \emph{The Annals of
Statistics} 9(1), 130--134.
}
\seealso{
Other diagnostics: 
\code{\link{bic_aic}()},
\code{\link{ess_summary}()},
\code{\link{ess_trace}()},
\code{\link{gmm_anneal_path}()},
\code{\link{gmm_conditional_entropy}()},
\code{\link{gmm_entropy}()},
\code{\link{gmm_evidence}()},
\code{\link{gmm_fit_quality}()},
\code{\link{gmm_independence_graph}()},
\code{\link{gmm_mutual_information}()},
\code{\link{hellinger_mc}()},
\code{\link{kld_trace}()},
\code{\link{proxy_functional_ci}()}
}
\concept{diagnostics}
