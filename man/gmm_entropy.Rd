% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/entropy.R
\name{gmm_entropy}
\alias{gmm_entropy}
\title{Differential entropy of a Gaussian mixture}
\usage{
gmm_entropy(g, order = c("renyi2", "shannon"), n_mc = 5000L, seed = NULL)
}
\arguments{
\item{g}{A \link{gmm} (or \link{gmm_fit}) object.}

\item{order}{\code{"renyi2"} (closed-form quadratic Renyi entropy, the default) or
\code{"shannon"} (Monte-Carlo estimate with an analytic upper bound).}

\item{n_mc}{Number of Monte Carlo samples for \code{order = "shannon"}.}

\item{seed}{Optional integer seed for the Monte Carlo draw.}
}
\value{
For \code{order = "renyi2"}, a numeric scalar. For \code{order = "shannon"}, a
list with components \code{mc} (the estimate), \code{mc_se} (its standard error),
\code{upper_bound} (the analytic upper bound), and \code{n_mc}.
}
\description{
Computes the differential entropy of a Gaussian mixture. The quadratic
(order-2) Renyi entropy \eqn{H_2(g) = -\log \int g(x)^2 \, dx} is available in
closed form, because \eqn{\int g^2} is a finite sum of Gaussian-density
evaluations. Shannon entropy has no closed form for a mixture (the integrand
carries the logarithm of a sum) and is estimated by Monte Carlo, reported
with its standard error and an analytic upper bound that brackets it from
above.
}
\examples{
g <- gmm(weights = c(0.5, 0.5),
         means = list(c(-2, 0), c(2, 0)),
         covariances = list(diag(2), diag(2)))
gmm_entropy(g)
gmm_entropy(g, order = "shannon", n_mc = 2000L, seed = 1L)
}
\seealso{
\code{\link[=gmm_divergence]{gmm_divergence()}}, \code{\link[=gmm_kld]{gmm_kld()}}

Other diagnostics: 
\code{\link{bic_aic}()},
\code{\link{ess_summary}()},
\code{\link{ess_trace}()},
\code{\link{gmm_anneal_path}()},
\code{\link{gmm_conditional_entropy}()},
\code{\link{gmm_independence_graph}()},
\code{\link{gmm_mutual_information}()},
\code{\link{hellinger_mc}()},
\code{\link{kld_trace}()}
}
\concept{diagnostics}
