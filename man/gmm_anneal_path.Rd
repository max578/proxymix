% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/anneal.R
\name{gmm_anneal_path}
\alias{gmm_anneal_path}
\title{Phase-transition component discovery by deterministic annealing}
\usage{
gmm_anneal_path(
  x,
  k_max = 8L,
  sigma = NULL,
  t_high = NULL,
  t_low = NULL,
  n_steps = 80L,
  n_inner = 30L,
  w = NULL,
  perturb = 0.02,
  merge_tol = 0.1,
  ridge_eps = 1e-06,
  seed = 1L
)
}
\arguments{
\item{x}{A numeric \code{n} by \code{p} matrix of samples, or a \link{gmm_target} carrying a
\code{samples} matrix. For regime (iii) targets, pass an importance-resampled
draw.}

\item{k_max}{Maximum number of centroids tracked (the discovered count is at
most \code{k_max}).}

\item{sigma}{Reference scale: the shared covariance is \code{sigma^2 * I}. When
\code{NULL} (the default) \code{sigma} is \code{1}, so the first critical temperature is
the largest eigenvalue of the data covariance.}

\item{t_high, t_low}{Top and bottom of the temperature schedule. When \code{NULL}
they default to \code{3 * t_critical_analytic} and \code{0.05 * t_critical_analytic},
bracketing the bifurcation cascade.}

\item{n_steps}{Number of temperatures on the geometric schedule.}

\item{n_inner}{Fixed-point iterations run at each temperature.}

\item{w}{Optional length-\code{n} vector of non-negative observation weights
(e.g. importance weights). Defaults to uniform.}

\item{perturb}{Symmetry-breaking perturbation, as a fraction of the data
scale, applied to the centroids at each temperature.}

\item{merge_tol}{Two centroids count as distinct when their distance exceeds
\code{merge_tol} times the data scale.}

\item{ridge_eps}{Ridge added to the reference covariance for stability.}

\item{seed}{Optional integer seed for the perturbations (the result is
deterministic given a seed).}
}
\value{
A list with elements \code{path} (a data frame of \code{temperature},
\code{n_effective} and \code{free_energy}), \code{critical_temperatures} (the temperatures
at which the count increased), \code{first_critical_temperature} (the first such,
or \code{NA} if none was detected), \code{t_critical_analytic}
(\eqn{\lambda_{\max}(\Sigma^{-1} C)}), \code{k_selected} (the widest-plateau
component count), \code{lambda_max} and \code{sigma}.
}
\description{
Tracks the number of distinct mixture centroids as a function of temperature
under mass-constrained deterministic annealing (Rose, Gurewitz and Fox 1990),
a physics-derived alternative to information-criterion model selection. The
system starts at a high temperature where all \code{k_max} centroids collapse to
the data centroid (a single effective component) and is cooled along a
geometric schedule; at each critical temperature a centroid bifurcates, so the
number of distinct centroids grows in steps. The temperatures at which it
grows are the phase transitions, and the count occupying the widest
temperature range is the discovered component number.
}
\details{
The first bifurcation has a closed-form critical temperature
\eqn{T_c = \lambda_{\max}(\Sigma^{-1} C)}, where \eqn{C} is the (weighted)
data covariance and \eqn{\Sigma = \sigma^2 I} the shared reference covariance.
This value is returned as \code{t_critical_analytic} and serves as an independent
analytic check on the empirically detected first transition. Subsequent
transitions have no comparably simple closed form, and the count is a
diagnostic rather than a guarantee.

Annealing fixes the component covariance to the reference \eqn{\Sigma} so the
temperature is the only scale; this is the clean isotropic regime in which the
critical temperature is exact. For robust \emph{fitting} under free covariances,
use \code{anneal = TRUE} on \code{\link[=fit_em_samples]{fit_em_samples()}} or \code{\link[=fit_kld_em]{fit_kld_em()}} instead.
}
\examples{
set.seed(1)
x <- rbind(
  matrix(stats::rnorm(120, mean = -4), ncol = 2),
  matrix(stats::rnorm(120, mean =  4), ncol = 2)
)
path <- gmm_anneal_path(x, k_max = 4L, n_steps = 40L)
path$k_selected
path$first_critical_temperature
}
\references{
Rose, K., Gurewitz, E. and Fox, G. C. (1990) Statistical
mechanics and phase transitions in clustering. \emph{Physical Review Letters}
65(8), 945--948. \doi{10.1103/PhysRevLett.65.945}
}
\seealso{
Other diagnostics: 
\code{\link{bic_aic}()},
\code{\link{ess_summary}()},
\code{\link{ess_trace}()},
\code{\link{gmm_conditional_entropy}()},
\code{\link{gmm_entropy}()},
\code{\link{gmm_mutual_information}()},
\code{\link{hellinger_mc}()},
\code{\link{kld_trace}()}
}
\concept{diagnostics}
