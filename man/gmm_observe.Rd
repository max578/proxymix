% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/operator_calculus.R
\name{gmm_observe}
\alias{gmm_observe}
\title{Bayesian update of a Gaussian mixture on a noisy linear observation}
\usage{
gmm_observe(g, A, y, noise_cov, b = 0, ridge_eps = 1e-06)
}
\arguments{
\item{g}{A \link{gmm} (or \link{gmm_fit}) in \code{R^p}.}

\item{A}{An \code{m} by \code{p} numeric matrix.}

\item{y}{A length-\code{m} numeric vector (the observation).}

\item{noise_cov}{An \code{m} by \code{m} SPD numeric matrix (the observation
noise covariance \code{R}). Required.}

\item{b}{Numeric scalar or length-\code{m} vector. Default \code{0}.}

\item{ridge_eps}{Tiny ridge added to updated covariances for numerical
hygiene. Set to \code{0} to disable.}
}
\value{
A \link{gmm} in \code{R^p} with the same number of components and the
reweighted component weights.
}
\description{
Conditions a Gaussian mixture \code{g} on a single noisy linear observation
\eqn{y = A x + b + \epsilon}, \eqn{\epsilon \sim \mathcal{N}(0, R)}.
Per component, applies the Kalman gain
\deqn{K_k = \Sigma_k A^\top S_k^{-1}, \qquad S_k = A \Sigma_k A^\top + R,}
and updates
\deqn{\mu'_k = \mu_k + K_k (y - A \mu_k - b), \qquad
      \Sigma'_k = (I - K_k A) \Sigma_k.}
Component weights are multiplied by the marginal evidence
\eqn{\pi_k \mathcal{N}(y; A \mu_k + b, S_k)} and renormalised. This is
the finite-mixture analogue of a Kalman update step.
}
\details{
If the marginal evidence vanishes at every component (e.g. \code{y} is
many standard deviations from every component), the function issues a
warning and returns \code{g} unchanged with \code{metadata$gmm_observe_no_update = TRUE}.
}
\examples{
g <- gmm(weights = c(0.5, 0.5),
         means = list(c(-1, 0), c(1, 0)),
         covariances = list(diag(2), diag(2)))
A <- matrix(c(1, 0), nrow = 1L)
gmm_observe(g, A = A, y = 0.8, noise_cov = matrix(0.25, 1, 1))
}
\seealso{
Other operators: 
\code{\link{gmm_affine}()},
\code{\link{gmm_aggregate}()},
\code{\link{gmm_missing}()}

Other v0_3: 
\code{\link{gmm_affine}()},
\code{\link{gmm_aggregate}()},
\code{\link{gmm_missing}()}
}
\concept{operators}
\concept{v0_3}
