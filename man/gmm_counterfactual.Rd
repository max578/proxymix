% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/intervene.R
\name{gmm_counterfactual}
\alias{gmm_counterfactual}
\title{Counterfactual law of one unit (abduction, action, prediction)}
\usage{
gmm_counterfactual(g, evidence, do, query, ridge_eps = 1e-06)
}
\arguments{
\item{g}{A \link{gmm} (or \link{gmm_fit}) in \code{R^p}.}

\item{evidence}{A length-\code{p} numeric vector of the observed unit.
Unobserved coordinates are \code{NA}; the \code{query} coordinate must be observed.}

\item{do}{A length-\code{p} numeric vector. Intervened coordinates take their
counterfactual value; the rest are \code{NA}. Intervened coordinates must be
observed in \code{evidence}.}

\item{query}{A single integer coordinate index in \code{seq_len(p)} -- the
outcome whose counterfactual is sought.}

\item{ridge_eps}{Tiny ridge added to the conditioning covariances for
numerical hygiene.}
}
\value{
A \code{gmm_counterfactual_law} object carrying the \code{K} atoms, their
abduction weights, and the identified counterfactual \code{mean}.
}
\description{
Computes the per-unit counterfactual of a \code{query} coordinate (the outcome)
for an observed unit, under a \code{do()} intervention -- Pearl's third rung on
the latent-class structural causal model read off a fitted Gaussian
mixture.
}
\details{
The three steps are closed form. \emph{Abduction} recovers the regime posterior
\eqn{\pi_k(\text{evidence})} of the observed unit and, within each
component, its structural residual. \emph{Action} sets the \code{do} coordinates.
\emph{Prediction} re-evaluates the query coordinate. For a binary treatment the
result is a discrete law on \code{K} atoms,
\deqn{Y_{t'} \mid (y, t, x) = \sum_k \pi_k(y, t, x)\,
      \delta\!\big(y + \beta_k^{T}(t' - t)\big),}
where \eqn{\beta_k^T} is component \code{k}'s within-class treatment slope. Only
the \strong{mean} of this law is identified; its spread reflects regime
uncertainty, not the (unidentified) cross-world coupling, so the variance
and tail accessors refuse to answer (see \code{\link[=gmm_cf_variance]{gmm_cf_variance()}}).
}
\examples{
## Observed unit (y = 1.2, t = 0, x = 0.5); imagine t = 1.
g <- gmm(weights = c(0.6, 0.4),
         means = list(c(0, 0, 0), c(2, 1, 1)),
         covariances = list(diag(3), diag(3)))
cf <- gmm_counterfactual(g, evidence = c(1.2, 0, 0.5),
                         do = c(NA, 1, NA), query = 1L)
cf@mean
}
\seealso{
\code{\link[=gmm_intervene]{gmm_intervene()}}, \code{\link[=gmm_cf_variance]{gmm_cf_variance()}}, \code{\link[=proxy_retrospective_uplift]{proxy_retrospective_uplift()}}

Other operators: 
\code{\link{gmm_affine}()},
\code{\link{gmm_aggregate}()},
\code{\link{gmm_convolve}()},
\code{\link{gmm_filter}()},
\code{\link{gmm_intervene}()},
\code{\link{gmm_missing}()},
\code{\link{gmm_mix}()},
\code{\link{gmm_observe}()},
\code{\link{gmm_product}()},
\code{\link{gmm_reduce}()}

Other decision: 
\code{\link{fit_uplift}()},
\code{\link{gmm_cf_mean}()},
\code{\link{gmm_cf_tail_prob}()},
\code{\link{gmm_cf_variance}()},
\code{\link{gmm_intervene}()},
\code{\link{proxy_cate}()},
\code{\link{proxy_confounding_gap}()},
\code{\link{proxy_decide}()},
\code{\link{proxy_identification_report}()},
\code{\link{proxy_overlap}()},
\code{\link{proxy_policy_value}()},
\code{\link{proxy_predict}()},
\code{\link{proxy_regime_segments}()},
\code{\link{proxy_retrospective_uplift}()},
\code{\link{proxy_uplift}()},
\code{\link{uplift_identification}()},
\code{\link{uplift_model}()}
}
\concept{decision}
\concept{operators}
