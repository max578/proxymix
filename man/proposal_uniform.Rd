% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/proposals.R
\name{proposal_uniform}
\alias{proposal_uniform}
\alias{proposal_mvn}
\alias{proposal_mvt}
\title{Preferred names for the importance-proposal constructors}
\usage{
proposal_uniform(n_dim, lower = -1, upper = 1)

proposal_mvn(n_dim, mean = rep(0, n_dim), cov = diag(n_dim))

proposal_mvt(n_dim, mean = rep(0, n_dim), sigma = diag(n_dim), df = 5)
}
\arguments{
\item{n_dim}{Ambient dimension \code{p}.}

\item{lower}{Length-\code{p} numeric vector of lower bounds (recycled from a
single value).}

\item{upper}{Length-\code{p} numeric vector of upper bounds (recycled from a
single value).}

\item{mean, cov}{Forwarded to \code{\link[=is_mvn]{is_mvn()}}.}

\item{sigma, df}{Forwarded to \code{\link[=is_mvt]{is_mvt()}}.}
}
\value{
An \link{is_proposal} object.
}
\description{
\code{proposal_uniform()}, \code{proposal_mvn()}, and \code{proposal_mvt()} are the
preferred names of \code{\link[=is_uniform]{is_uniform()}}, \code{\link[=is_mvn]{is_mvn()}}, and \code{\link[=is_mvt]{is_mvt()}}: the
historical \verb{is_*} prefix reads as a logical predicate, which these
constructors are not. The \verb{is_*} names remain available as aliases and
are not scheduled for removal.
}
\examples{
proposal_uniform(n_dim = 2L, lower = -5, upper = 5)
proposal_mvn(n_dim = 2L, mean = c(0, 0), cov = diag(2))
proposal_mvt(n_dim = 2L, df = 5)   # heavier tails; a robust default
}
\seealso{
Other proposals: 
\code{\link{is_mvn}()},
\code{\link{is_mvt}()},
\code{\link{is_uniform}()}
}
\concept{proposals}
