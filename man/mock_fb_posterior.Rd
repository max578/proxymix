% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/from_fb_posterior.R
\name{mock_fb_posterior}
\alias{mock_fb_posterior}
\title{A synthetic C4 producer for testing the seam}
\usage{
mock_fb_posterior(
  shape = c("gaussian", "banana"),
  n_dim = 2L,
  sd = 1,
  unnormalised = FALSE
)
}
\arguments{
\item{shape}{One of \code{"gaussian"} (default) or \code{"banana"}.}

\item{n_dim}{Ambient dimension for \code{shape = "gaussian"}. Ignored (fixed at
2) for \code{shape = "banana"}.}

\item{sd}{Standard deviation of the isotropic Gaussian (\code{shape = "gaussian"}). Default \code{1}.}

\item{unnormalised}{If \code{TRUE}, the returned log-density carries an arbitrary
additive constant and \code{log_normalizer} is set so the absolute KLD remains
recoverable. If \code{FALSE} (the default), the log-density is exactly
normalised and \code{log_normalizer} is \code{0}.}
}
\value{
An \code{fb_log_posterior_spec}.
}
\description{
Returns a conforming \code{fb_log_posterior_spec} whose log-density is a
known target, so the C4 consumer path can be exercised end-to-end with no
flexyBayes installed. It is the stand-in for a fitted flexyBayes object
until the B4 producer lands: the spec it returns is byte-for-byte the shape
\code{\link[=from_fb_posterior]{from_fb_posterior()}} expects from the real producer.
}
\details{
Two shapes are provided. \code{"gaussian"} is an isotropic \code{n_dim}-dimensional
Gaussian with a known normaliser, against which proxy recovery and absolute
KLD can be checked exactly. \code{"banana"} is the warped 2-D Gaussian of
\code{\link[=banana_target]{banana_target()}}, a non-Gaussian target that genuinely needs more than one
mixture component -- the realistic compression case.
}
\examples{
spec <- mock_fb_posterior(shape = "gaussian", n_dim = 2L)
spec
## A non-Gaussian target needing several components.
mock_fb_posterior(shape = "banana")
}
\seealso{
\code{\link[=from_fb_posterior]{from_fb_posterior()}}, \code{\link[=fb_log_posterior_spec]{fb_log_posterior_spec()}}.

Other interop: 
\code{\link{fb_log_posterior_spec}()},
\code{\link{fb_producer_available}()},
\code{\link{from_fb_posterior}()},
\code{\link{gmm_target_from_posterior}()}
}
\concept{interop}
