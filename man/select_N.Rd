% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/select_n.R
\name{select_N}
\alias{select_N}
\title{Select the number of mixture components}
\usage{
select_N(
  target,
  candidates = 1:6,
  regime = c("auto", "sample", "kld"),
  seed = NULL,
  ...
)
}
\arguments{
\item{target}{A \link{gmm_target}.}

\item{candidates}{Integer vector of component counts to try.}

\item{regime}{\code{"auto"} (default: \code{"sample"} when the target carries
samples, \code{"kld"} otherwise), \code{"sample"}, or \code{"kld"}.}

\item{seed}{Optional integer seed shared across candidates (paired
fits).}

\item{...}{Forwarded to the regime's fitter (\code{\link[=fit_em_samples]{fit_em_samples()}} or
\code{\link[=fit_kld_em]{fit_kld_em()}}).}
}
\value{
A list of class \code{proxymix_selection}: \code{best_n} (the chosen
count), \code{best_fit} (its \link{gmm_fit}), and \code{table} (one row per
candidate with the criterion values).
}
\description{
Fits every candidate component count and chooses one by a
regime-appropriate criterion. With samples (regime ii) the choice is
the smallest BIC. With an evaluable-only target (regime iii) each
candidate is scored by its held-out validation KLD -- an independent
importance draw the fit never trained on -- and the choice follows the
one-standard-error rule: the smallest \code{N} whose validation score is
within one Monte Carlo standard error of the best score. The scored
table is returned alongside the choice, and callers who prefer to
choose by eye can ignore the recommendation.
}
\examples{
sel <- select_N(banana_target(), candidates = 1:3,
                is_size = 1500L, max_iter = 20L, seed = 1L)
sel$table
sel$best_n
}
\seealso{
Other fitting: 
\code{\link{fit_em_samples}()},
\code{\link{fit_kld_em}()},
\code{\link{fit_moment_match}()},
\code{\link{from_kde}()},
\code{\link{from_objective}()}
}
\concept{fitting}
