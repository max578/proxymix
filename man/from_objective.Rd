% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/from_objective.R
\name{from_objective}
\alias{from_objective}
\title{Map the optima of an objective with a Gaussian-mixture proxy}
\usage{
from_objective(
  objective,
  lower,
  upper,
  N = NULL,
  minimise = TRUE,
  temperature = NULL,
  n_steps = 6L,
  exploration = 0.5,
  inflate = 1.8,
  is_size = 10000L,
  max_iter = 70L,
  ridge_eps = 1e-04,
  seed = NULL
)
}
\arguments{
\item{objective}{Function taking a length-\code{p} numeric vector and
returning a finite numeric scalar -- the objective to minimise (or
maximise; see \code{minimise}).}

\item{lower, upper}{Numeric vectors of equal length \code{p} giving the box
over which the optima are sought. Every \code{upper} must exceed its
\code{lower}.}

\item{N}{Number of mixture components. Default \code{max(10L, 5L * p)}. Raise
it for objectives with many optima or strong symmetry.}

\item{minimise}{Logical. If \code{TRUE} (the default) lower \code{objective}
values are better (the proxy concentrates on the minima); if \code{FALSE}
the proxy concentrates on the maxima.}

\item{temperature}{Optional control of the cooling ladder. \code{NULL} (the
default) derives a ladder automatically from a uniform probe of the
landscape. A positive scalar sets the final (lowest) temperature; a
length-2 numeric \code{c(high, low)} sets both ends explicitly.}

\item{n_steps}{Number of temperatures in the cooling ladder. Default
\code{6L}.}

\item{exploration}{Probability mass the importance proposal places on
uniform exploration of the box at each step, in \verb{[0, 1]}. Default
\code{0.5}. Larger values explore more (and starve no basin); smaller values
exploit the basins found so far.}

\item{inflate}{Factor by which the current mixture covariances are
inflated when used as the exploitation part of the proposal. Default
\code{1.8}.}

\item{is_size}{Importance-sample size per cooling step. Default \code{1e4L}.}

\item{max_iter}{Maximum EM iterations per cooling step. Default \code{70L}.}

\item{ridge_eps}{Ridge added to each component covariance at every
M-step. Forwarded to \code{\link[=fit_kld_em]{fit_kld_em()}}.}

\item{seed}{Optional integer seed for reproducibility.}
}
\value{
A \link{gmm_fit} (the fitted proxy) carrying a \code{from_objective}
metadata record with the temperature ladder and box. Pass it to
\code{\link[=gmm_modes]{gmm_modes()}} to extract the distinct optima.
}
\description{
Fits a Gaussian-mixture proxy to the Gibbs measure
\eqn{\exp(-f(x) / T)} of a user-supplied objective \code{f} over a bounded
box, by cooling a short temperature ladder through regime-(iii)
importance-sampled KLD-EM (\code{\link[=fit_kld_em]{fit_kld_em()}}). As the temperature falls the
mixture mass concentrates on the low regions of \code{f}, so the fitted
mixture is a closed-form \emph{map} over the optima rather than a single
point estimate. Pair it with \code{\link[=gmm_modes]{gmm_modes()}} to read off the distinct
optima.
}
\details{
The Gibbs measure can be evaluated point-wise but not directly sampled,
which is precisely the setting of regime (iii): minimising
the Kullback-Leibler divergence from a Gaussian mixture to a peaked
target is the rank-weighted Gaussian update at the heart of \code{\link[=fit_kld_em]{fit_kld_em()}}.
This function is that fit, driven against a sequence of cooling Gibbs
targets and warm-started from the previous fit at each step. Because a
multimodal \code{f} produces a multimodal target, the components spread across
the basins and recover the optima together.

Recovery is most reliable with component headroom -- a number of
components \code{N} comfortably larger than the number of optima you expect,
so the defensive proposal can keep a component on each basin. Symmetric
landscapes (where several optima are exchangeable) need the most headroom.

Dimensional scope. As with the rest of regime (iii), the importance-
sampling effective sample size falls sharply with dimension; the guard is
\code{p <= 5} (recommended), \code{p <= 10} (allowed with a warning), \code{p > 10}
(rejected).
}
\examples{
## A bimodal 1-D objective with minima at +/- 2.
f <- function(v) (v[1]^2 - 4)^2
fit <- from_objective(f, lower = -5, upper = 5, N = 6L,
                      is_size = 2000L, n_steps = 5L, seed = 1L)
gmm_modes(fit)$modes
}
\seealso{
\code{\link[=gmm_modes]{gmm_modes()}} to resolve the fitted map into distinct optima.

Other fitting: 
\code{\link{fit_em_samples}()},
\code{\link{fit_kld_em}()},
\code{\link{fit_moment_match}()},
\code{\link{from_fb_posterior}()},
\code{\link{from_kde}()}
}
\concept{fitting}
