% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/operator_calculus.R
\name{gmm_aggregate}
\alias{gmm_aggregate}
\title{Aggregation pushforward of a Gaussian mixture}
\usage{
gmm_aggregate(g, A, noise_cov = NULL, ridge_eps = 1e-06)
}
\arguments{
\item{g}{A \link{gmm} (or \link{gmm_fit}) in \code{R^p}.}

\item{A}{An \code{m} by \code{p} numeric matrix.}

\item{noise_cov}{Optional \code{m} by \code{m} SPD numeric matrix. Default
\code{NULL} (deterministic aggregation).}

\item{ridge_eps}{Tiny ridge added to the output covariances for
numerical hygiene.}
}
\value{
A \link{gmm} in \code{R^m}.
}
\description{
A named alias for \code{\link[=gmm_affine]{gmm_affine()}} when \code{A} is a (row-wise) aggregation
matrix — e.g. a block-sum, block-average, or unequal-weight aggregation
used in downscaling pipelines. The mathematics is identical to
\code{\link[=gmm_affine]{gmm_affine()}}; the alias gives the public API a clearer hook for
aggregation-specific diagnostics in later releases.
}
\examples{
g <- gmm(weights = c(0.5, 0.5),
         means = list(c(-1, 0, 1), c(1, 0, -1)),
         covariances = list(diag(3), diag(3)))
# Sum coordinates 1 and 2 into a single aggregate; pass coord 3 through.
A <- matrix(c(1, 1, 0,
              0, 0, 1), nrow = 2L, byrow = TRUE)
gmm_aggregate(g, A)
}
\seealso{
Other operators: 
\code{\link{gmm_affine}()},
\code{\link{gmm_convolve}()},
\code{\link{gmm_counterfactual}()},
\code{\link{gmm_filter}()},
\code{\link{gmm_intervene}()},
\code{\link{gmm_missing}()},
\code{\link{gmm_mix}()},
\code{\link{gmm_observe}()},
\code{\link{gmm_product}()},
\code{\link{gmm_reduce}()}
}
\concept{operators}
