% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gmm_target_constructors.R
\name{banana_target}
\alias{banana_target}
\title{Banana-shaped 2-D target}
\usage{
banana_target(with_samples = FALSE, n = 2000L, seed = 1L)
}
\arguments{
\item{with_samples}{If \code{TRUE}, attach \code{n} exact samples drawn by the
change-of-variables trick. Default \code{FALSE} — the target then exposes
only its \code{log_density}, which is the regime-(iii) use case.}

\item{n}{Number of samples to attach when \code{with_samples = TRUE}.}

\item{seed}{Optional integer seed used when drawing the samples.}
}
\value{
A \link{gmm_target} in dimension 2.
}
\description{
A 2-D "banana" density obtained by warping an isotropic Gaussian
through the map \eqn{(z_1, z_2) \mapsto (z_1, z_2 + \tfrac{1}{2}(z_1^2 - 1))}.
The map has unit Jacobian, so the resulting density is exactly normalised.
}
\examples{
b <- banana_target()
b
b@log_density(matrix(c(0, 0, 1, 0), ncol = 2, byrow = TRUE))
}
\seealso{
Other targets: 
\code{\link{donut_target}()},
\code{\link{gmm_target_from_samples}()},
\code{\link{mixture_target}()}
}
\concept{targets}
