% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/impute.R
\name{gmm_impute}
\alias{gmm_impute}
\title{Multiple imputation by Gaussian-mixture conditioning}
\usage{
gmm_impute(
  data,
  N = NULL,
  m = 20L,
  mechanism = mar(),
  seed = NULL,
  max_iter = 100L,
  tol = 1e-06,
  ridge_eps = 1e-06
)
}
\arguments{
\item{data}{A numeric matrix or data frame with \code{NA} for missing entries.}

\item{N}{Number of mixture components. \code{NULL} (the default) selects it by
the Bayesian information criterion over \code{1:6}.}

\item{m}{Number of completed datasets to draw. Default \code{20L}.}

\item{mechanism}{A missingness mechanism: \code{\link[=mar]{mar()}}, \code{\link[=censored]{censored()}}, or \code{\link[=mnar]{mnar()}}.
The string \code{"mar"} is also accepted. Default \code{\link[=mar]{mar()}}.}

\item{seed}{Optional integer seed. When supplied the result is
reproducible and the ambient random-number state is restored on exit.}

\item{max_iter}{Maximum EM iterations per fit. Default \code{100L}.}

\item{tol}{Relative log-likelihood tolerance for EM convergence. Default
\code{1e-6}.}

\item{ridge_eps}{Ridge added to each component covariance at every M-step.
Default \code{1e-6}.}
}
\value{
A \link{gmm_imputation} object.
}
\description{
Fits a Gaussian mixture to a numeric dataset that contains missing values
and draws \code{m} completed datasets from the mixture conditional
\eqn{p(x_{\mathrm{missing}} \mid x_{\mathrm{observed}})}. Because the
mixture can be multimodal and heteroscedastic, the imputations follow the
shape of the joint distribution rather than a single Gaussian, which keeps
downstream inference valid on data that a single-Gaussian or
linear-Gaussian imputer mis-specifies.
}
\details{
Imputation is conditioning. For a row with observed coordinates the
missing coordinates follow the closed-form mixture conditional (the same
Schur-complement algebra as \code{\link[=gmm_conditionalise]{gmm_conditionalise()}}). The mixture is fitted
to the incomplete data by expectation-maximisation whose E-step uses each
row's observed margin and whose M-step restores the conditional covariance
of the filled entries, so component variances are not under-estimated.

Proper multiple imputation requires the fitting parameters themselves to
carry uncertainty, otherwise the pooled intervals are too narrow. Each of
the \code{m} imputations is therefore drawn under a mixture fitted to an
independent bootstrap resample of the rows, so \code{\link[=proxy_pool]{proxy_pool()}} reflects both
imputation and parameter uncertainty.

The \code{mechanism} says how an entry came to be missing, which sets the
conditional the missing value is drawn from: \code{\link[=mar]{mar()}} (the default) for missing
at random, \code{\link[=censored]{censored()}} for a known interval such as a detection limit, or
\code{\link[=mnar]{mnar()}} for a value-dependent selection model. The interval and
value-dependent gates act on a single coordinate, and a row missing that
coordinate must have its other coordinates observed. Numeric data only;
categorical variables are out of scope.
}
\examples{
set.seed(1)
x1 <- rnorm(200)
x2 <- x1 + rnorm(200)
x2[runif(200) < plogis(x1)] <- NA          # missing at random on x1
imp <- gmm_impute(cbind(x1, x2), N = 1L, m = 10L, seed = 1L)
proxy_pool(imp, "x2")$estimate             # pooled mean of x2
}
\seealso{
\code{\link[=gmm_complete]{gmm_complete()}} to extract completions, \code{\link[=proxy_pool]{proxy_pool()}} to pool an
estimand across them, \code{\link[=gmm_conditionalise]{gmm_conditionalise()}} for the conditioning algebra.

Other imputation: 
\code{\link{as_mids}()},
\code{\link{gmm_complete}()},
\code{\link{gmm_imputation}()},
\code{\link{mechanism}},
\code{\link{proxy_fmi}()},
\code{\link{proxy_mnar_sensitivity}()},
\code{\link{proxy_pool}()}
}
\concept{imputation}
