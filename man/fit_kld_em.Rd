% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_kld_em.R
\name{fit_kld_em}
\alias{fit_kld_em}
\title{Importance-sampled KLD-EM fit (regime iii)}
\usage{
fit_kld_em(
  target,
  N = 3L,
  proposal = NULL,
  is_size = 5000L,
  init = NULL,
  max_iter = 100L,
  tol = 1e-05,
  ridge_eps = 1e-06,
  min_ess = 50,
  on_low_ess = c("warn", "abort"),
  seed = NULL,
  validation_size = NULL,
  validation_proposal = NULL,
  validation_seed = NULL,
  support_warn = TRUE,
  adapt = c("none", "pmc"),
  refresh_every = 5L,
  defensive_gamma = 0.15,
  inflate = 1.5,
  anneal = FALSE,
  temp_schedule = NULL,
  canonicalise = TRUE
)
}
\arguments{
\item{target}{A \link{gmm_target} with a non-NULL \code{log_density}.}

\item{N}{Number of mixture components.}

\item{proposal}{An \link{is_proposal}. When \code{NULL} (the default) the proposal is
chosen automatically: a support-matched \code{\link[=is_uniform]{is_uniform()}} when the target
declares a bounded or one-sided \code{support}, otherwise a multivariate-t with
\code{df = 5} in \code{target@n_dim} dimensions. The automatic choice is announced
with a one-line message so it is never silent.}

\item{is_size}{Number of importance-sampling draws used for fitting.}

\item{init}{A \link{gmm} initialisation, or \code{NULL} to use a kmeans pass on
the importance-resampled draws.}

\item{max_iter}{Maximum number of EM iterations.}

\item{tol}{Convergence tolerance on the relative change in the
importance-weighted EM objective \verb{Q(theta) = sum_n W_n log g(x_n)}.
\code{Q} is invariant to the target's normalising constant, so the
stopping rule behaves identically for normalised and unnormalised
targets (the importance-sampled KLD estimate carries an additive
\verb{-log Z(f)} offset and is therefore never used for stopping).}

\item{ridge_eps}{Ridge added to each component covariance at every
M-step.}

\item{min_ess}{Minimum effective sample size below which the fit is
flagged as degenerate: a classed warning (\code{proxymix_low_ess}) is
issued (or, with \code{on_low_ess = "abort"}, a classed error
\code{proxymix_degenerate_fit}), the fit's \code{converged} flag is forced to
\code{FALSE}, and \code{degenerate = TRUE} is recorded in the diagnostics and
the quality certificate.}

\item{on_low_ess}{What to do when the effective sample size falls below
\code{min_ess}: \code{"warn"} (the default) flags and continues, \code{"abort"}
refuses to return a degenerate fit.}

\item{seed}{Optional integer seed. When supplied, the fit is
reproducible end-to-end: the fitting IS draw, the initialisation
resample and kmeans pass, and any empty-component reseed draws are
all derived from it. When \code{NULL}, those draws consume the ambient
random-number stream.}

\item{validation_size}{Number of independent importance-sampling draws
to use for held-out validation. The default \code{NULL} uses
\code{ceiling(is_size / 4)}, so the overfit-vs-generalise diagnostic
(\code{validation_kld} and the certificate's \code{validation_gap}) exists by
default; set \code{0L} to disable the validation split.}

\item{validation_proposal}{Optional \link{is_proposal} for the validation
sample. Defaults to the same proposal used for fitting.}

\item{validation_seed}{Optional integer seed used when drawing the
validation sample. Defaults to \code{seed + 1L} when \code{seed} is supplied,
\code{NULL} otherwise.}

\item{support_warn}{Logical. If \code{TRUE} (the default), issue a warning
when more than 5\% of IS draws receive non-finite weights (typically
because the proposal does not dominate the target's support).}

\item{adapt}{Proposal adaptation: \code{"none"} (the default; one fixed IS
draw, the historical behaviour) or \code{"pmc"} (population-Monte-Carlo
refresh of the proposal from the current iterate; see Details).}

\item{refresh_every}{With \code{adapt = "pmc"}, refresh the proposal after
this many EM iterations on the current batch. Default \code{5L}.}

\item{defensive_gamma}{With \code{adapt = "pmc"}, the mass kept on the
original proposal as a heavy-tailed defensive anchor at every
refresh (bounds the importance-weight variance). Default \code{0.15}.}

\item{inflate}{With \code{adapt = "pmc"}, the factor inflating the current
iterate's covariances inside the refreshed proposal. Default \code{1.5}.}

\item{anneal}{Logical. If \code{TRUE}, a deterministic-annealing warm-start
(see \code{\link[=gmm_anneal_path]{gmm_anneal_path()}}) replaces the kmeans initialisation: components
are annealed from a high temperature down to one on the importance-weighted
draws, and the resulting parameters seed the (unchanged) cold KLD-EM loop.
This attacks the local-optima sensitivity of cold EM. Defaults to \code{FALSE}.}

\item{temp_schedule}{Optional numeric vector of descending temperatures for
the annealing warm-start. \code{NULL} (the default) uses a geometric schedule
from \code{10} down to \code{1} in covariance-whitened units. Ignored when
\code{anneal = FALSE}.}

\item{canonicalise}{Logical. If \code{TRUE} (the default), the fitted
mixture is post-processed by \code{\link[=gmm_canonicalise]{gmm_canonicalise()}}.}
}
\value{
A \link{gmm_fit} with \code{regime = "kld"}. The diagnostics list
contains, among others, \code{kld_trace}, \code{kld_final},
\code{kld_is_shifted}, \code{kld_final_absolute} (when computable), \code{ess},
\code{ess_relative} (\code{ess / is_size}), \code{max_weight}, \code{support_fraction},
\code{mc_se_kld}, \code{validation_kld}, \code{validation_ess}, and
\code{validation_max_weight}.
}
\description{
Implements regime (iii) of Hoek and Elliott (2024). Minimises
\code{KL(f || g_theta)} where \code{f} is supplied as an evaluable log-density on
the target, via expectation-maximisation against importance-sampled
draws from a user-chosen proposal \code{q}.
}
\details{
With \code{adapt = "none"} (the default) the Monte Carlo draws from \code{q} are
computed once at the start and the resulting self-normalised
importance-sampling weights are reused at every EM iteration. With
\code{adapt = "pmc"} the proposal is refreshed every \code{refresh_every}
iterations with a defensive mixture built from the current iterate --
the population-Monte-Carlo scheme: the fitted mixture (covariances
inflated by \code{inflate}) carries \code{1 - defensive_gamma} of the proposal
mass and the original proposal \code{q} keeps \code{defensive_gamma} as a
heavy-tailed anchor, a fresh IS batch is drawn, and EM continues on the
refreshed weights. Because the refreshed proposal tracks the target,
the effective sample size recovers from a poor initial proposal and the
usable dimension range extends well beyond what a fixed proposal
reaches; the per-batch ESS trace is reported as
\code{diagnostics$ess_history}. While a batch is degenerate (its effective
sample size is below \code{min_ess}), the refresh fires every iteration
with an escalating covariance inflation floored at a growing fraction
of the batch's sample covariance, so a collapsed iterate walks back
out toward the target instead of freezing; and convergence is only
accepted on an adapted batch, so a run that stabilises on the original
proposal's draw is refreshed at least once before it is allowed to
stop. The scheme is the mixture population-Monte-Carlo idea of Cappé
et al. (2008) with the defensive-mixture safeguard of Owen and Zhou
(2000); it re-draws rather than recycles batches (compare the adaptive
multiple importance sampling of Cornuet et al., 2012).

Since v0.1.1 the function also draws an \emph{independent} validation IS
sample when \code{validation_size > 0} and reports its own KLD estimate,
effective sample size, and largest weight share. This lets users tell
the difference between in-sample EM overfit to one particular IS draw
and a fit that generalises across independent IS draws.

When the target's \code{normalised} property is \code{FALSE} or \code{NA}, the
importance-sampled \code{kld_final} and \code{kld_trace} measure
\eqn{\widehat{KL}(f \Vert g) - \log Z(f)} rather than the absolute
divergence. The fit's diagnostics list records this via
\code{kld_is_shifted = TRUE} and a \code{kld_shift_explanation} string. When the
target also supplies a finite \code{log_normalizer}, a corrected absolute
estimate is reported as \code{kld_final_absolute}.
}
\examples{
tgt <- banana_target()
q <- is_mvt(n_dim = 2L, mean = c(0, 0),
            sigma = 4 * diag(2), df = 5)
fit <- fit_kld_em(tgt, N = 3L, proposal = q,
                  is_size = 1500L, max_iter = 25L, seed = 1L,
                  validation_size = 1500L)
fit@diagnostics$kld_final
fit@diagnostics$validation_kld
}
\references{
Cappé, O., Douc, R., Guillin, A., Marin, J.-M. and
Robert, C. P. (2008) Adaptive importance sampling in general mixture
classes. \emph{Statistics and Computing} 18, 447--459.
\doi{10.1007/s11222-008-9059-x}

Cornuet, J.-M., Marin, J.-M., Mira, A. and Robert, C. P. (2012)
Adaptive multiple importance sampling. \emph{Scandinavian Journal of
Statistics} 39, 798--812. \doi{10.1111/j.1467-9469.2011.00756.x}

Owen, A. and Zhou, Y. (2000) Safe and effective importance sampling.
\emph{Journal of the American Statistical Association} 95(449), 135--143.
\doi{10.1080/01621459.2000.10473909}
}
\seealso{
Other fitting: 
\code{\link{fit_em_samples}()},
\code{\link{fit_moment_match}()},
\code{\link{from_kde}()},
\code{\link{from_objective}()},
\code{\link{select_N}()}
}
\concept{fitting}
