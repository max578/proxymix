% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/entropy.R
\name{gmm_independence_graph}
\alias{gmm_independence_graph}
\title{Conditional-independence (Gaussian graphical model) structure of a mixture}
\usage{
gmm_independence_graph(g, threshold = 0.05)
}
\arguments{
\item{g}{A \link{gmm} (or \link{gmm_fit}) mixture.}

\item{threshold}{Non-negative partial-correlation magnitude above which an edge
is drawn. Defaults to \code{0.05}.}
}
\value{
A symmetric integer adjacency matrix (\code{1} = edge, \code{0} = none) with the
coordinate names of \code{g}, carrying the partial-correlation matrix as the
\code{"pcor"} attribute.
}
\description{
Returns the undirected second-order conditional-independence graph of a fitted
Gaussian mixture: the partial-correlation (Gaussian graphical model) structure
of the mixture's overall covariance. An edge \eqn{i - j} is present when the
partial correlation of coordinates \eqn{i} and \eqn{j} given all the others
exceeds \code{threshold} in magnitude, and absent when it does not -- the latter is
the Markov statement \eqn{x_i \perp x_j \mid x_{\mathrm{rest}}} at second order.
The overall covariance
\deqn{\mathrm{Cov}(X) = \sum_k w_k (\Sigma_k + \mu_k \mu_k^\top) - \bar\mu\,\bar\mu^\top,
  \qquad \bar\mu = \sum_k w_k \mu_k,}
is closed-form in the mixture parameters, so no sampling is required.
}
\details{
This is a graphical-model (dependency-structure) diagnostic, not a causal
discovery method: it recovers the undirected Markov skeleton, not edge
directions. Its distinctive use is \strong{regime (iii)}: composed with
\code{\link[=fit_kld_em]{fit_kld_em()}}, it recovers the dependency structure of a target you can only
\emph{evaluate} (an unnormalised energy / Gibbs density), where no sample exists to
hand a sampling-based estimator. Being second order, it sees dependencies that
enter the covariance; a purely higher-order coupling (zero correlation, nonzero
dependence) is not detected -- raise the mixture's component count, or read the
coordinate-block dependence with \code{\link[=gmm_mutual_information]{gmm_mutual_information()}} instead.
}
\examples{
## Regime (iii): the Markov structure of an evaluable-but-unsampleable density.
## A continuous chain field x1 - x2 - x3 (couplings only between neighbours).
energy <- function(X) {
  X <- matrix(X, ncol = 3)
  rowSums((X^2 - 1)^2) - 0.7 * (X[, 1] * X[, 2] + X[, 2] * X[, 3])
}
target <- gmm_target(n_dim = 3L, log_density = function(X) -energy(X))
g <- fit_kld_em(target, N = 8L, proposal = is_uniform(3L, -3, 3),
                is_size = 8000L, anneal = TRUE, seed = 1L, support_warn = FALSE)
gmm_independence_graph(g)            # recovers x1 - x2 - x3 (no x1 - x3 edge)
}
\seealso{
\code{\link[=gmm_mutual_information]{gmm_mutual_information()}}, \code{\link[=fit_kld_em]{fit_kld_em()}}, \code{\link[=gmm_conditionalise]{gmm_conditionalise()}}

Other diagnostics: 
\code{\link{bic_aic}()},
\code{\link{ess_summary}()},
\code{\link{ess_trace}()},
\code{\link{gmm_anneal_path}()},
\code{\link{gmm_conditional_entropy}()},
\code{\link{gmm_entropy}()},
\code{\link{gmm_mutual_information}()},
\code{\link{hellinger_mc}()},
\code{\link{kld_trace}()}
}
\concept{diagnostics}
