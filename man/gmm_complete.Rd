% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/impute.R
\name{gmm_complete}
\alias{gmm_complete}
\title{Extract completed datasets from a \code{gmm_imputation}}
\usage{
gmm_complete(object, which = 1L)
}
\arguments{
\item{object}{A \link{gmm_imputation}.}

\item{which}{Either an integer vector of imputation indices, or \code{"all"}
for every completion. Default \code{1L}.}
}
\value{
When \code{which} selects one completion, a single completed dataset
(matrix, or data frame if the input was one); otherwise a list of them.
}
\description{
Extract completed datasets from a \code{gmm_imputation}
}
\examples{
x1 <- rnorm(60)
x2 <- x1 + rnorm(60, sd = 0.4)
x2[sample(60, 10)] <- NA
imp <- gmm_impute(cbind(x1, x2), N = 1L, m = 10L, seed = 1L)
first <- gmm_complete(imp, which = 1L)   # one completed dataset
anyNA(first)
all_completions <- gmm_complete(imp, which = "all")   # a list of ten
length(all_completions)
}
\seealso{
Other imputation: 
\code{\link{as_mids}()},
\code{\link{gmm_imputation}()},
\code{\link{gmm_impute}()},
\code{\link{mechanism}},
\code{\link{proxy_fmi}()},
\code{\link{proxy_mnar_sensitivity}()},
\code{\link{proxy_pool}()}
}
\concept{imputation}
