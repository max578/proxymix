% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/intervene.R
\name{gmm_intervene}
\alias{gmm_intervene}
\title{Interventional law of a Gaussian mixture (the do-operator)}
\usage{
gmm_intervene(g, do, given = NULL, ridge_eps = 1e-06)
}
\arguments{
\item{g}{A \link{gmm} (or \link{gmm_fit}) in \code{R^p}.}

\item{do}{A length-\code{p} numeric vector. Coordinates to intervene on take
their \code{do}-value; coordinates not intervened on are \code{NA}.}

\item{given}{A length-\code{p} numeric vector, or \code{NULL} (the default, meaning
no conditioning). Coordinates to condition on take their value;
coordinates not conditioned on are \code{NA}. A coordinate may not appear in
both \code{do} and \code{given}.}

\item{ridge_eps}{Tiny ridge added to the conditional covariances for
numerical hygiene. Set to \code{0} to disable.}
}
\value{
A \link{gmm} over the free coordinates (those \code{NA} in both \code{do} and
\code{given}), with weights re-gated by the \code{given} evidence only.
}
\description{
Reads a fitted Gaussian mixture as a latent-class structural causal model
and returns the interventional distribution of the free coordinates under
\code{do()} of some coordinates, optionally conditioning on others.
}
\details{
Intervened (\code{do}) coordinates are \emph{set} inside every component but do not
re-weight the regime gate -- this is the graph surgery that distinguishes
\eqn{p(\cdot \mid do(T = t))} from \eqn{p(\cdot \mid T = t)}. Conditioned
(\code{given}) coordinates re-weight the gate in the usual Bayesian way. Writing
the component prior as \eqn{\pi_k}, the within-component conditional mean as
\eqn{\mu_k}, and the given-coordinate evidence as \eqn{e_k}, the returned
mixture has weights \eqn{\pi_k(given) \propto \pi_k\, e_k} and per-component
parameters from the Schur conditional on the union of the \code{do} and \code{given}
coordinates.

For a joint fit over \eqn{(Y, T, X)}, \verb{gmm_intervene(fit, do = T = 1, given = X = x)} is the do-response \eqn{p(Y \mid do(T = 1), X = x)}; its mean
is \eqn{\sum_k \pi_k(x)\, \mu_k^{y \mid 1, x}}, the latent-confounder-mode
interventional mean of \link{proxy_cate}.
}
\examples{
## Joint (Y, T, X): set T = 1 while conditioning on X = 0.3.
g <- gmm(weights = c(0.5, 0.5),
         means = list(c(0, 0, 0), c(2, 1, 1)),
         covariances = list(diag(3), diag(3)))
gmm_intervene(g, do = c(NA, 1, NA), given = c(NA, NA, 0.3))
}
\seealso{
\code{\link[=gmm_counterfactual]{gmm_counterfactual()}}, \code{\link[=proxy_cate]{proxy_cate()}}

Other operators: 
\code{\link{gmm_affine}()},
\code{\link{gmm_aggregate}()},
\code{\link{gmm_counterfactual}()},
\code{\link{gmm_filter}()},
\code{\link{gmm_missing}()},
\code{\link{gmm_observe}()},
\code{\link{gmm_reduce}()}

Other decision: 
\code{\link{fit_uplift}()},
\code{\link{gmm_cf_mean}()},
\code{\link{gmm_cf_tail_prob}()},
\code{\link{gmm_cf_variance}()},
\code{\link{gmm_counterfactual}()},
\code{\link{proxy_cate}()},
\code{\link{proxy_confounding_gap}()},
\code{\link{proxy_decide}()},
\code{\link{proxy_identification_report}()},
\code{\link{proxy_overlap}()},
\code{\link{proxy_policy_value}()},
\code{\link{proxy_predict}()},
\code{\link{proxy_regime_segments}()},
\code{\link{proxy_retrospective_uplift}()},
\code{\link{proxy_uplift}()},
\code{\link{uplift_identification}()},
\code{\link{uplift_model}()}
}
\concept{decision}
\concept{operators}
