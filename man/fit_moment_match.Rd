% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_moment_match.R
\name{fit_moment_match}
\alias{fit_moment_match}
\title{Closed-form moment-matching fit}
\usage{
fit_moment_match(target, N = 1L, ridge_eps = 1e-06, canonicalise = TRUE)
}
\arguments{
\item{target}{A \link{gmm_target}.}

\item{N}{Number of components. \code{N >= 2} returns a moment-seeded mixture
without iterative refinement.}

\item{ridge_eps}{Ridge added to the empirical covariance for numerical
stability.}

\item{canonicalise}{Logical. If \code{TRUE} (the default), the fitted
mixture is post-processed by \code{\link[=gmm_canonicalise]{gmm_canonicalise()}} so that components
are sorted by descending weight and (as a tiebreaker) by descending
\verb{||mu||}. Set \code{FALSE} to retain the raw component order.}
}
\value{
A \link{gmm_fit} with \code{regime = "moment"}.
}
\description{
Implements regime (i) of Hoek and Elliott (2024). When \code{N == 1}, this is
the exact moment match: \code{mu} is the target mean and \code{Sigma} is the target
covariance. When \code{N > 1}, the function returns the deterministic
moment-seed of \code{\link[=init_moment_seed]{init_moment_seed()}} wrapped as a \link{gmm_fit}, without
iterative refinement — useful as a starting point for the iterative
regimes.
}
\details{
Either the \code{target} must carry an \code{n} by \code{p} \code{samples} matrix, or its
\code{metadata} slot must contain pre-computed \code{moments} of the form
\verb{list(mean = <p-vec>, cov = <p-by-p>)}.
}
\examples{
x <- matrix(stats::rnorm(200), ncol = 2)
tgt <- gmm_target_from_samples(x)
fit_moment_match(tgt, N = 1L)
}
\seealso{
Other fitting: 
\code{\link{fit_em_samples}()},
\code{\link{fit_kld_em}()},
\code{\link{from_fb_posterior}()},
\code{\link{from_kde}()}
}
\concept{fitting}
