% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gmm_ops.R
\name{gmm_kld}
\alias{gmm_kld}
\title{Kullback-Leibler divergence between two Gaussian mixtures}
\usage{
gmm_kld(p, q, n_mc = 5000L, variational = TRUE)
}
\arguments{
\item{p, q}{Two \link{gmm} (or \link{gmm_fit}) objects of the same ambient
dimension.}

\item{n_mc}{Number of Monte Carlo samples drawn from \code{p}.}

\item{variational}{If \code{TRUE}, also return the Hershey--Olsen variational
approximation.}
}
\value{
A list with components
\itemize{
\item \code{mc} - the Monte Carlo estimate of \code{KL(p || q)},
\item \code{mc_se} - its Monte Carlo standard error,
\item \code{variational} - the variational approximation (\code{NA} if
\code{variational = FALSE}),
\item \code{n_mc} - the number of Monte Carlo samples used.
}
}
\description{
Estimates \code{KL(p || q)} between two Gaussian mixtures by Monte Carlo and,
optionally, evaluates the Hershey--Olsen variational approximation as a
deterministic sanity check.
}
\details{
The Monte Carlo estimator draws \code{n_mc} samples from \code{p} and returns the
empirical mean of \verb{log p(x) - log q(x)}, together with a Monte Carlo
standard error.

The variational approximation is
\deqn{\widehat{D}_{\mathrm{var}}(p \Vert q) =
  \sum_a \pi_a \log\!\left(\frac{\sum_{a'} \pi_{a'} \, e^{-\mathrm{KL}(p_a \Vert p_{a'})}}{\sum_b \omega_b \, e^{-\mathrm{KL}(p_a \Vert q_b)}}\right),}
which is exact when \code{p == q} and tends to be a usable lower bound when
the components of \code{p} and \code{q} are well-separated. The closed-form
Gaussian--Gaussian KL \code{KL(p_a || q_b)} is used internally.
}
\examples{
p <- gmm(weights = c(0.5, 0.5),
         means = list(c(-1, 0), c(1, 0)),
         covariances = list(diag(2), diag(2)))
q <- gmm(weights = 1,
         means = list(c(0, 0)),
         covariances = list(diag(2) * 2))
gmm_kld(p, q, n_mc = 500L)
}
\seealso{
Other ops: 
\code{\link{dgmm}()},
\code{\link{gmm_canonicalise}()},
\code{\link{gmm_conditionalise}()},
\code{\link{gmm_divergence}()},
\code{\link{gmm_marginalise}()},
\code{\link{gmm_modes}()},
\code{\link{rgmm}()}
}
\concept{ops}
