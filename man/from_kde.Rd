% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/from_kde.R
\name{from_kde}
\alias{from_kde}
\title{Compile a kernel-density estimate into a Gaussian-mixture proxy}
\usage{
from_kde(
  samples,
  N = 3L,
  bandwidth = "silverman",
  proposal = NULL,
  is_size = 5000L,
  max_iter = 100L,
  tol = 1e-05,
  ridge_eps = 1e-06,
  min_ess = 50L,
  seed = NULL,
  validation_size = 0L,
  validation_proposal = NULL,
  validation_seed = NULL,
  support_warn = TRUE,
  canonicalise = TRUE
)
}
\arguments{
\item{samples}{An \code{n} by \code{p} numeric matrix of points. \code{n >= 5},
\code{p <= 10}.}

\item{N}{Number of mixture components in the proxy.}

\item{bandwidth}{Either \code{"silverman"}, \code{"scott"}, a positive numeric
scalar (absolute bandwidth applied to every coordinate), or a
length-\code{p} positive numeric vector of per-coordinate absolute
bandwidths. Default \code{"silverman"}.}

\item{proposal}{Optional \link{is_proposal}. Default is a multivariate-t
centred at \code{colMeans(samples)}, scale = ridge(cov(samples)) +
diag(h^2), \code{df = 5}.}

\item{is_size}{Importance-sample size for fitting. Default \code{5000L}.}

\item{max_iter}{Maximum EM iterations. Forwarded to \code{\link[=fit_kld_em]{fit_kld_em()}}.}

\item{tol}{Convergence tolerance. Forwarded to \code{\link[=fit_kld_em]{fit_kld_em()}}.}

\item{ridge_eps}{Ridge added to each component covariance at every
M-step. Forwarded to \code{\link[=fit_kld_em]{fit_kld_em()}}.}

\item{min_ess}{Minimum effective sample size below which a warning is
issued. Forwarded to \code{\link[=fit_kld_em]{fit_kld_em()}}.}

\item{seed}{Optional integer seed for the fitting IS draw.}

\item{validation_size}{Held-out IS sample size. Forwarded to
\code{\link[=fit_kld_em]{fit_kld_em()}}.}

\item{validation_proposal}{Optional \link{is_proposal} for the held-out
sample. Forwarded to \code{\link[=fit_kld_em]{fit_kld_em()}}.}

\item{validation_seed}{Optional integer seed for the held-out IS draw.
Forwarded to \code{\link[=fit_kld_em]{fit_kld_em()}}.}

\item{support_warn}{Logical. Forwarded to \code{\link[=fit_kld_em]{fit_kld_em()}}.}

\item{canonicalise}{Logical. If \code{TRUE}, the fitted mixture is
post-processed by \code{\link[=gmm_canonicalise]{gmm_canonicalise()}}. Forwarded to \code{\link[=fit_kld_em]{fit_kld_em()}}.}
}
\value{
A \link{gmm_fit} with \code{regime = "kld"} and metadata recording the
KDE inputs (\code{kde_samples_n}, \code{bandwidth}, \code{bandwidth_method}).
}
\description{
Fits an \code{N}-component Gaussian-mixture proxy to a (Gaussian, diagonal-
bandwidth) kernel-density estimate over \code{samples}, via regime (iii)
KLD-EM. The proxy is closed-form marginalisable, conditionable, and
samplable; the KDE is none of those things on its own.
}
\details{
This is a \strong{compression} operation: take an \code{n}-sample KDE and replace
it with the closest \code{N}-component mixture in the Kullback-Leibler sense
(which is much smaller than \code{n} for typical use). Bias inherited from
the KDE is reproduced in the proxy; the bandwidth controls the
bias-variance trade-off.

Dimensional scope. The graduation guard is \code{p <= 5} (recommended),
\code{p <= 10} (allowed with warning), \code{p > 10} (rejected). The wedge of
KLD-EM is regime (iii) IS, whose effective-sample-size collapses
sharply in high dimensions; richer ambient spaces will await the v0.3
affine-Gaussian operator calculus.
}
\examples{
set.seed(1L)
x <- rbind(
  mvnfast::rmvn(120L, mu = c(-2, 0), sigma = diag(2)),
  mvnfast::rmvn(120L, mu = c( 2, 0), sigma = diag(2))
)
fit <- from_kde(x, N = 2L, is_size = 2000L, max_iter = 40L, seed = 1L)
fit
ess_summary(fit)
}
\seealso{
Other fitting: 
\code{\link{fit_em_samples}()},
\code{\link{fit_kld_em}()},
\code{\link{fit_moment_match}()},
\code{\link{from_fb_posterior}()}

Other v0_2: 
\code{\link{gmm_target_from_posterior}()}
}
\concept{fitting}
\concept{v0_2}
