% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decide.R
\name{proxy_cate}
\alias{proxy_cate}
\title{Heterogeneous treatment effects (CATE / uplift)}
\usage{
proxy_cate(
  model,
  newdata,
  t1 = NULL,
  t0 = NULL,
  se = TRUE,
  se_method = c("delta", "mc"),
  level = 0.95,
  B = 200L,
  scale = c("link", "response"),
  threshold = 0.5,
  ...
)
}
\arguments{
\item{model}{An \link{uplift_model}.}

\item{newdata}{A data frame carrying the covariate columns.}

\item{t1, t0}{The treated and control treatment values. Default the treatment levels observed at fit time (\code{model@treatment_levels}); a value matching neither observed level aborts.}

\item{se}{Logical -- compute standard errors and confidence intervals.}

\item{se_method}{One of \code{"delta"} (closed form, the default) or \code{"mc"}
(resampling).}

\item{level}{Confidence level for the interval. Default \code{0.95}.}

\item{B}{Number of bootstrap refits when \code{se_method = "mc"}. Default \code{200}.}

\item{scale}{One of \code{"link"} (the latent / continuous scale, the default) or
\code{"response"}. For a binary outcome the response scale reports the effect on
the discretised predictive probability \code{P(Y > threshold)}; for continuous
and count outcomes the two scales coincide.}

\item{threshold}{Decision threshold for the binary discretised predictive.
Default \code{0.5}.}

\item{...}{Forwarded to \code{\link[=fit_proxymix]{fit_proxymix()}} inside the \code{"mc"} refits.}
}
\value{
A \link[data.table:data.table]{data.table::data.table} with columns \code{id}, \code{tau}, \code{se},
\code{ci_lo}, \code{ci_hi}, \code{overlap_flag}.
}
\description{
Per-unit conditional average treatment effect \eqn{\tau(x) = E[Y \mid
do(T = t_1), X = x] - E[Y \mid do(T = t_0), X = x]}, read in closed form off
the fitted joint mixture. Under the model's default \code{"ignorability"}
assumption this is the contrast of two component-gated conditional means;
under \code{"latent_confounder"} it is the regime-gated within-class slope (the
do-operator). The two coincide when treatment carries no information about
the regime beyond \code{X}; their difference is \code{\link[=proxy_confounding_gap]{proxy_confounding_gap()}}.
}
\details{
The default delta-method standard error is the within-component prediction
variance, holding the regime gate fixed; it reduces to the ordinary
least-squares standard error of the treatment effect at K = 1. Set
\code{se_method = "mc"} for a resampling standard error that also reflects gate
uncertainty.
}
\examples{
set.seed(1)
n <- 400L
x <- stats::rnorm(n)
t <- stats::rbinom(n, 1L, 0.5)
y <- 1 + 0.5 * x + (1 + x) * t + stats::rnorm(n, sd = 0.5)
dat <- data.frame(y = y, t = t, x = x)
m <- fit_uplift(dat, "y", "t", "x", N = 2L, regime = "sample",
                max_iter = 50L, seed = 1L)
proxy_cate(m, newdata = data.frame(x = c(-1, 0, 1)))
}
\seealso{
\code{\link[=proxy_decide]{proxy_decide()}}, \code{\link[=proxy_confounding_gap]{proxy_confounding_gap()}}, \code{\link[=proxy_overlap]{proxy_overlap()}}

Other decision: 
\code{\link{fit_uplift}()},
\code{\link{gmm_cf_mean}()},
\code{\link{gmm_cf_tail_prob}()},
\code{\link{gmm_cf_variance}()},
\code{\link{gmm_counterfactual}()},
\code{\link{gmm_intervene}()},
\code{\link{proxy_confounding_gap}()},
\code{\link{proxy_decide}()},
\code{\link{proxy_identification_report}()},
\code{\link{proxy_overlap}()},
\code{\link{proxy_policy_value}()},
\code{\link{proxy_predict}()},
\code{\link{proxy_regime_segments}()},
\code{\link{proxy_retrospective_uplift}()},
\code{\link{proxy_uplift}()},
\code{\link{uplift_identification}()},
\code{\link{uplift_model}()}
}
\concept{decision}
