% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/proxymix-classes.R
\name{is_proposal}
\alias{is_proposal}
\title{An importance-sampling proposal}
\usage{
is_proposal(
  n_dim = integer(0),
  sample = NULL,
  log_density = NULL,
  name = "is_proposal",
  metadata = list()
)
}
\arguments{
\item{n_dim}{Integer scalar — the ambient dimension \code{p} (the property is
called \code{n_dim} rather than \code{dim} because S7 reserves \code{dim} as an
attribute name).}

\item{sample}{Function: \verb{function(n)} returning an \code{n} by \code{p} numeric
matrix of independent draws.}

\item{log_density}{Function: \verb{function(x)} taking a numeric matrix \code{n} by
\code{p} and returning a length-\code{n} numeric vector of \verb{log q(x)}.}

\item{name}{Human-readable name.}

\item{metadata}{Optional list of additional descriptors.}
}
\value{
An S7 object of class \code{is_proposal}.
}
\description{
An \code{is_proposal} packages a sampler with its corresponding log-density.
Pass one to \code{\link[=fit_kld_em]{fit_kld_em()}} (or \code{\link[=fit_proxymix]{fit_proxymix()}} with \code{regime = "kld"})
to plug an alternative proposal into the regime-(iii) loop.
}
\examples{
q <- is_mvn(n_dim = 2L, mean = c(0, 0), cov = diag(2))
q
}
\seealso{
Other classes: 
\code{\link{gmm}()},
\code{\link{gmm_dim}()},
\code{\link{gmm_fit}()},
\code{\link{gmm_n_components}()},
\code{\link{gmm_target}()}
}
\concept{classes}
