% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/uplift.R
\name{fit_uplift}
\alias{fit_uplift}
\title{Fit an uplift / next-best-action model from a data frame}
\usage{
fit_uplift(
  data,
  outcome,
  treatment,
  covariates,
  N = "auto",
  regime = "auto",
  assume = c("ignorability", "latent_confounder"),
  outcome_type = c("continuous", "binary", "count"),
  n_grid = 1:4,
  seed = NULL,
  ...
)
}
\arguments{
\item{data}{A data frame holding the outcome, treatment and covariate
columns.}

\item{outcome}{A single column name -- the outcome \code{Y}.}

\item{treatment}{A single column name -- the binary treatment \code{T}.}

\item{covariates}{A character vector of one or more column names -- the
pre-treatment covariates \code{X}.}

\item{N}{Number of mixture components, or \code{"auto"} (the default) to select
by BIC over \code{n_grid}.}

\item{regime}{One of \code{"auto"}, \code{"moment"}, \code{"sample"}, \code{"kld"}, forwarded to
\code{\link[=fit_proxymix]{fit_proxymix()}}. The default \code{"auto"} uses classical EM on the supplied
rows.}

\item{assume}{One of \code{"ignorability"} (the default) or
\code{"latent_confounder"} -- the identification regime the effects are read
under. See \code{\link[=proxy_cate]{proxy_cate()}} and \code{\link[=proxy_confounding_gap]{proxy_confounding_gap()}}.}

\item{outcome_type}{One of \code{"continuous"} (the default), \code{"binary"} or
\code{"count"}. Effects are reported on the response scale via a discretised
predictive for the non-continuous types; see \code{\link[=proxy_cate]{proxy_cate()}}.}

\item{n_grid}{Integer vector of candidate component counts used when
\code{N = "auto"}. Default \code{1:4}.}

\item{seed}{Optional integer. When supplied, the fitting (including any
random EM starts) runs under a fixed seed and the global RNG state is
restored on exit, so the fit is reproducible without disturbing the
caller's stream.}

\item{...}{Additional arguments forwarded to \code{\link[=fit_proxymix]{fit_proxymix()}} (e.g.
\code{max_iter}, \code{n_starts}, \code{ridge_eps}).}
}
\value{
An \link{uplift_model}.
}
\description{
Assembles a joint Gaussian-mixture proxy over the outcome, the treatment,
and the covariates, and returns an \link{uplift_model} that the decision verbs
read in closed form. One fit yields prediction, heterogeneous treatment
effects, optimal actions, off-line policy value, and an identification
audit -- see \code{\link[=proxy_cate]{proxy_cate()}}, \code{\link[=proxy_decide]{proxy_decide()}}, \code{\link[=proxy_policy_value]{proxy_policy_value()}} and
\code{\link[=proxy_identification_report]{proxy_identification_report()}}.
}
\details{
The component count \code{N} may be fixed or chosen automatically. With
\code{N = "auto"} the function sweeps \code{n_grid} and keeps the K that minimises the
joint BIC; the full BIC trace is stored in the model's metadata. The
treatment is binary at this version (\verb{\{t0, t1\}}); a continuous dose is a
future extension.
}
\examples{
set.seed(1)
n <- 400L
x <- stats::rnorm(n)
t <- stats::rbinom(n, 1L, 0.5)
y <- 1 + 0.5 * x + (1 + x) * t + stats::rnorm(n, sd = 0.5)
dat <- data.frame(y = y, t = t, x = x)
m <- fit_uplift(dat, outcome = "y", treatment = "t", covariates = "x",
                N = 2L, regime = "sample", max_iter = 50L, seed = 1L)
m
}
\seealso{
\code{\link[=proxy_cate]{proxy_cate()}}, \code{\link[=proxy_decide]{proxy_decide()}}, \code{\link[=proxy_identification_report]{proxy_identification_report()}}

Other decision: 
\code{\link{gmm_cf_mean}()},
\code{\link{gmm_cf_tail_prob}()},
\code{\link{gmm_cf_variance}()},
\code{\link{gmm_counterfactual}()},
\code{\link{gmm_intervene}()},
\code{\link{proxy_cate}()},
\code{\link{proxy_confounding_gap}()},
\code{\link{proxy_decide}()},
\code{\link{proxy_identification_report}()},
\code{\link{proxy_overlap}()},
\code{\link{proxy_policy_value}()},
\code{\link{proxy_predict}()},
\code{\link{proxy_regime_segments}()},
\code{\link{proxy_retrospective_uplift}()},
\code{\link{proxy_uplift}()},
\code{\link{uplift_identification}()},
\code{\link{uplift_model}()}
}
\concept{decision}
