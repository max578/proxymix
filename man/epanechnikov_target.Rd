% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gmm_target_constructors.R
\name{epanechnikov_target}
\alias{epanechnikov_target}
\title{Compact-support Epanechnikov target}
\usage{
epanechnikov_target(
  n_dim = 1L,
  center = 0,
  half_width = 1,
  with_samples = FALSE,
  n = 2000L,
  seed = 1L
)
}
\arguments{
\item{n_dim}{Ambient dimension \code{p}.}

\item{center}{Length-1 (recycled) or length-\code{p} numeric centre per
coordinate.}

\item{half_width}{Length-1 (recycled) or length-\code{p} positive numeric
half-width per coordinate.}

\item{with_samples}{If \code{TRUE}, attach \code{n} exact samples drawn by the
Devroye (1986) three-uniform method. Default \code{FALSE}.}

\item{n}{Number of samples to attach when \code{with_samples = TRUE}.}

\item{seed}{Optional integer seed used when drawing the samples.}
}
\value{
A \link{gmm_target} in dimension \code{n_dim} with a declared compact
\code{support}.
}
\description{
The canonical compact-support target: a product of one-dimensional
Epanechnikov densities
\eqn{K(u) = \tfrac{3}{4}(1 - u^2)\,\mathbf{1}_{|u| \le 1}}, rescaled to
\verb{[center - half_width, center + half_width]} in each coordinate. No mixture
of full-support Gaussians can have compact support, so this target is the
clean case where regime (iii) is the only viable fitting route. It declares
its \code{support}, which makes \code{\link[=fit_kld_em]{fit_kld_em()}} (and \code{\link[=fit_proxymix]{fit_proxymix()}} with
\code{regime = "kld"}) select a support-matched uniform proposal automatically
instead of the default multivariate-t, which would place importance mass
where the log-density is \code{-Inf}.
}
\examples{
e <- epanechnikov_target()
e
e@log_density(matrix(c(0, 0.5, 1.5), ncol = 1L)) # finite, finite, -Inf
}
\seealso{
Other targets: 
\code{\link{banana_target}()},
\code{\link{donut_target}()},
\code{\link{gmm_target_from_samples}()},
\code{\link{maxent_target}()},
\code{\link{mixture_target}()}
}
\concept{targets}
