% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/from_objective.R
\name{gmm_modes}
\alias{gmm_modes}
\title{Modes of a Gaussian mixture}
\usage{
gmm_modes(object, starts = NULL, tol = 1e-05, dedup = NULL, max_iter = 200L)
}
\arguments{
\item{object}{A \link{gmm} or \link{gmm_fit}.}

\item{starts}{Optional \code{m}-by-\code{p} matrix of starting points for the
mean-shift. Default \code{NULL} uses the component means.}

\item{tol}{Convergence tolerance on the mean-shift step length. Default
\code{1e-5}.}

\item{dedup}{Distance below which two converged points are treated as the
same mode. Default \code{NULL} derives a small fraction of the spread of the
component means.}

\item{max_iter}{Maximum mean-shift iterations per start. Default \code{200L}.}
}
\value{
A list with \code{modes} (an \code{n}-by-\code{p} matrix of distinct modes,
ordered by descending mixture density), \code{density} (the mixture density
at each mode), and \code{n} (the number of modes).
}
\description{
Returns the distinct local modes of a Gaussian-mixture density by
Gaussian mean-shift (the fixed-point hill-climb of Carreira-Perpinan
2000) started from each component mean, together with the mixture density
at each mode. Nearby converged points are merged so that each genuine
mode is reported once.
}
\details{
A mixture of \code{K} components has at most \code{K} modes, and fewer when
components overlap; mean-shift from every component mean finds them
robustly without a grid. The companion of \code{\link[=from_objective]{from_objective()}}: applied to
the fitted map it returns the recovered optima (ordered by density, which
for a Gibbs proxy ranks the deepest optima first).
}
\examples{
g <- gmm(
  weights = rep(1 / 3, 3),
  means = list(c(-3, 0), c(3, 0), c(0, 4)),
  covariances = rep(list(0.3 * diag(2)), 3)
)
gmm_modes(g)$modes
}
\seealso{
\code{\link[=from_objective]{from_objective()}}, whose fitted map this resolves into optima.

Other ops: 
\code{\link{dgmm}()},
\code{\link{gmm_canonicalise}()},
\code{\link{gmm_conditionalise}()},
\code{\link{gmm_divergence}()},
\code{\link{gmm_kld}()},
\code{\link{gmm_marginalise}()},
\code{\link{gmm_mean}()},
\code{\link{pgmm}()},
\code{\link{rgmm}()}
}
\concept{ops}
