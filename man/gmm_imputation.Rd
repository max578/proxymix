% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/impute.R
\name{gmm_imputation}
\alias{gmm_imputation}
\title{A Gaussian-mixture multiple-imputation result}
\usage{
gmm_imputation(
  data = NULL,
  completions = list(),
  fits = list(),
  point_fit = NULL,
  n_components = integer(0),
  m = integer(0),
  mechanism = "mar",
  observed = NULL,
  var_names = character(0),
  is_data_frame = FALSE,
  diagnostics = list(),
  call = NULL
)
}
\arguments{
\item{data}{The numeric data matrix supplied to \code{\link[=gmm_impute]{gmm_impute()}}, with \code{NA}
for missing entries.}

\item{completions}{List of \code{m} completed data matrices.}

\item{fits}{List of \code{m} bootstrap-fitted \link{gmm} objects behind the
completions.}

\item{point_fit}{The \link{gmm} fitted to the full data.}

\item{n_components}{Integer number of mixture components.}

\item{m}{Integer number of completions.}

\item{mechanism}{Missingness mechanism (currently \code{"mar"}).}

\item{observed}{Logical matrix marking the observed entries.}

\item{var_names}{Character vector of column names.}

\item{is_data_frame}{Logical; whether the input was a data frame.}

\item{diagnostics}{List of fit diagnostics (per-column missing rates,
convergence, iterations).}

\item{call}{The matched call.}
}
\value{
An S7 object of class \code{gmm_imputation}.
}
\description{
The object returned by \code{\link[=gmm_impute]{gmm_impute()}}. It carries the \code{m} completed data
matrices, the bootstrap-fitted mixtures behind them (used by the analytic
pooling in \code{\link[=proxy_pool]{proxy_pool()}}), the mixture fitted to the full data, and a
record of the missingness. Pass it to \code{\link[=gmm_complete]{gmm_complete()}} to extract the
completed datasets and to \code{\link[=proxy_pool]{proxy_pool()}} / \code{\link[=proxy_fmi]{proxy_fmi()}} for inference.
}
\examples{
x1 <- rnorm(60)
x2 <- x1 + rnorm(60, sd = 0.4)
x2[sample(60, 10)] <- NA
imp <- gmm_impute(cbind(x1, x2), N = 1L, m = 10L, seed = 1L)
imp   # an S7 object of class gmm_imputation
}
\seealso{
Other imputation: 
\code{\link{as_mids}()},
\code{\link{gmm_complete}()},
\code{\link{gmm_impute}()},
\code{\link{mechanism}},
\code{\link{proxy_fmi}()},
\code{\link{proxy_mnar_sensitivity}()},
\code{\link{proxy_pool}()}
}
\concept{imputation}
