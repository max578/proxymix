% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/identification.R
\name{proxy_policy_value}
\alias{proxy_policy_value}
\title{Off-line value of a targeting policy}
\usage{
proxy_policy_value(
  model,
  newdata,
  policy,
  value,
  cost = 0,
  t1 = 1,
  t0 = 0,
  exclude_low_overlap = TRUE
)
}
\arguments{
\item{model}{An \link{uplift_model}.}

\item{newdata}{A data frame carrying the covariate columns -- the population
the policy would be deployed on.}

\item{policy}{A per-unit action specification: a 0/1 vector of length
\code{nrow(newdata)}, a function of the \code{\link[=proxy_cate]{proxy_cate()}} table returning actions,
or one of the strings \code{"all"}, \code{"none"}, \code{"optimal"}.}

\item{value}{Numeric scalar -- the value of one unit of outcome.}

\item{cost}{Numeric scalar -- the cost of treating one unit. Default \code{0}.}

\item{t1, t0}{The treated and control treatment values. Default \code{1} and \code{0}.}

\item{exclude_low_overlap}{Logical -- drop overlap-flagged units from the
average (and report the count). Default \code{TRUE}.}
}
\value{
A one-row \link[data.table:data.table]{data.table::data.table} with columns \code{policy_value},
\code{n_used}, \code{n_excluded}, \code{n_treated}.
}
\description{
Estimates the expected value of deploying a per-unit targeting policy,
\eqn{V(d) = E_X[\,\text{value}\cdot E[Y \mid do(T = d(X)), X] - \text{cost}
\cdot d(X)\,]}, from the fitted model alone -- no live A/B test. Units that
fail the overlap diagnostic are excluded by default and their count is
reported, never silently dropped.
}
\examples{
set.seed(1)
n <- 600L
x <- stats::rnorm(n)
t <- stats::rbinom(n, 1L, 0.5)
y <- 1 + (0.4 + x) * t + stats::rnorm(n, sd = 0.5)
dat <- data.frame(y = y, t = t, x = x)
m <- fit_uplift(dat, "y", "t", "x", N = 2L, regime = "sample",
                max_iter = 80L, seed = 1L)
nd <- data.frame(x = stats::rnorm(200))
proxy_policy_value(m, nd, policy = "optimal", value = 1, cost = 0.3)
}
\seealso{
\code{\link[=proxy_decide]{proxy_decide()}}, \code{\link[=proxy_overlap]{proxy_overlap()}}

Other decision: 
\code{\link{fit_uplift}()},
\code{\link{gmm_cf_mean}()},
\code{\link{gmm_cf_tail_prob}()},
\code{\link{gmm_cf_variance}()},
\code{\link{gmm_counterfactual}()},
\code{\link{gmm_intervene}()},
\code{\link{proxy_cate}()},
\code{\link{proxy_confounding_gap}()},
\code{\link{proxy_decide}()},
\code{\link{proxy_identification_report}()},
\code{\link{proxy_overlap}()},
\code{\link{proxy_predict}()},
\code{\link{proxy_regime_segments}()},
\code{\link{proxy_retrospective_uplift}()},
\code{\link{proxy_uplift}()},
\code{\link{uplift_identification}()},
\code{\link{uplift_model}()}
}
\concept{decision}
