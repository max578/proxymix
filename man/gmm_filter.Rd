% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter.R
\name{gmm_filter}
\alias{gmm_filter}
\title{Bounded Gaussian-sum filtering over an observation series}
\usage{
gmm_filter(
  prior,
  dynamics,
  measurement,
  y,
  k_max = NULL,
  reduce = c("merge", "anneal"),
  ridge_eps = 1e-08
)
}
\arguments{
\item{prior}{A \link{gmm} (or \link{gmm_fit}) in \eqn{\mathbb{R}^p}: the initial belief.}

\item{dynamics}{A list \code{list(A =, b =, Q =)} describing the predict step, or
a function \verb{function(t)} returning such a list for time-varying dynamics.
\code{A} is the \code{p}-by-\code{p} state-transition matrix; \code{b} is an optional length-\code{p}
offset (default \code{0}); \code{Q} is the process noise -- a \code{p}-by-\code{p} covariance
matrix, a \link{gmm} on \eqn{\mathbb{R}^p} (a Gaussian-sum process noise), or
\code{NULL} (a deterministic predict).}

\item{measurement}{A list \code{list(C =, R =, d =)} describing the update step,
or a function \verb{function(t)} returning such a list. \code{C} is the \code{m}-by-\code{p}
observation matrix; \code{R} is the measurement noise -- an \code{m}-by-\code{m}
covariance matrix or a \link{gmm} on \eqn{\mathbb{R}^m} (a Gaussian-sum
measurement noise); \code{d} is an optional length-\code{m} offset (default \code{0}).}

\item{y}{The observations: an \code{n}-by-\code{m} numeric matrix, a length-\code{n} list of
length-\code{m} numeric vectors, or (when \code{m = 1}) a numeric vector of length
\code{n}.}

\item{k_max}{Optional component cap. \code{NULL} (the default) disables reduction
and runs the exact Kalman / Gaussian-sum filter; a positive integer caps
the component count after each step via \code{\link[=gmm_reduce]{gmm_reduce()}}.}

\item{reduce}{The reduction method passed to \code{\link[=gmm_reduce]{gmm_reduce()}} when \code{k_max} is
set: \code{"merge"} (the default, a deterministic moment-preserving merge) or
\code{"anneal"}.}

\item{ridge_eps}{Tiny ridge passed to \code{\link[=gmm_affine]{gmm_affine()}} and \code{\link[=gmm_observe]{gmm_observe()}} for
numerical hygiene. Set to \code{0} for an exact (ridge-free) recursion.}
}
\value{
A list with elements
\describe{
\item{\code{filtered}}{a length-\code{n} list of the filtered \link{gmm} beliefs, one
per step (after predict, update and any reduction).}
\item{\code{mean}}{an \code{n}-by-\code{p} matrix of the filtered mixture means.}
\item{\code{cov}}{a length-\code{n} list of the \code{p}-by-\code{p} filtered mixture
covariances.}
\item{\code{summary}}{a data frame with one row per step: the step index, the
per-coordinate filtered mean (\code{mean_1}, ...) and standard deviation
(\code{sd_1}, ...), the component count \code{n_components}, and the step log
marginal evidence \code{log_evidence} (whose sum over steps is the log
likelihood of the series).}
}
}
\description{
Runs a Gaussian-sum filter over a series of \code{n} observations by alternating
the predict operator \code{\link[=gmm_affine]{gmm_affine()}}, the update operator \code{\link[=gmm_observe]{gmm_observe()}} and
an optional reduction \code{\link[=gmm_reduce]{gmm_reduce()}}. At one component this is the classical
Kalman filter; at several components, with Gaussian-mixture process or
measurement noise, it is the Gaussian-sum filter of Alspach and Sorenson
(1972). The filter is a pure composition of the existing closed-form
operators; nothing here leaves the affine-Gaussian world.
}
\details{
At step \eqn{t} the belief is propagated through the linear-Gaussian dynamics
\eqn{x_t = A x_{t-1} + b + w}, then conditioned on the observation
\eqn{y_t = C x_t + d + v}, then (if \code{k_max} is set) reduced back to at most
\code{k_max} components. With Gaussian noise the component count is constant and
the recursion is the Kalman filter. Non-Gaussian noise is supplied as a
Gaussian sum -- a \link{gmm} in place of the covariance matrix \code{Q} or \code{R}. Each
such mixture multiplies the component count by its own component count every
step, so a long horizon is only runnable with reduction enabled. Reduction is
moment-preserving (see \code{\link[=gmm_reduce]{gmm_reduce()}}), so the filtered mean and covariance
are unaffected to the moment-matching order while the count stays bounded.
}
\examples{
## A one-dimensional local-level filter (random walk observed in noise).
prior <- gmm(weights = 1, means = list(0), covariances = list(matrix(1)))
truth <- cumsum(stats::rnorm(20, sd = 0.3))
y <- truth + stats::rnorm(20, sd = 0.5)
out <- gmm_filter(
  prior,
  dynamics    = list(A = matrix(1), Q = matrix(0.09)),
  measurement = list(C = matrix(1), R = matrix(0.25)),
  y = y
)
head(out$summary)

## Heavy-tailed process noise as a two-component Gaussian sum, capped at
## four components per step.
q_noise <- gmm(weights = c(0.9, 0.1),
               means = list(0, 0),
               covariances = list(matrix(0.05), matrix(1)))
out2 <- gmm_filter(
  prior,
  dynamics    = list(A = matrix(1), Q = q_noise),
  measurement = list(C = matrix(1), R = matrix(0.25)),
  y = y, k_max = 4L
)
max(out2$summary$n_components)
}
\references{
Alspach, D. L. and Sorenson, H. W. (1972) Nonlinear Bayesian
estimation using Gaussian sum approximations. \emph{IEEE Transactions on
Automatic Control} 17(4), 439--448. \doi{10.1109/TAC.1972.1100034}
}
\seealso{
Other operators: 
\code{\link{gmm_affine}()},
\code{\link{gmm_aggregate}()},
\code{\link{gmm_convolve}()},
\code{\link{gmm_counterfactual}()},
\code{\link{gmm_intervene}()},
\code{\link{gmm_missing}()},
\code{\link{gmm_mix}()},
\code{\link{gmm_observe}()},
\code{\link{gmm_product}()},
\code{\link{gmm_reduce}()}
}
\concept{operators}
