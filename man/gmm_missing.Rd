% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/operator_calculus.R
\name{gmm_missing}
\alias{gmm_missing}
\title{Condition a Gaussian mixture on the exact values of some coordinates}
\usage{
gmm_missing(g, observed, values)
}
\arguments{
\item{g}{A \link{gmm} (or \link{gmm_fit}) in \code{R^p}.}

\item{observed}{Integer vector of indices in \code{seq_len(p)}. The
coordinates to condition on (fully observed).}

\item{values}{Numeric vector of length \code{length(observed)}. The
observed values, in the same order as \code{observed}.}
}
\value{
A \link{gmm} in \code{R^(p - length(observed))}.
}
\description{
A structured wrapper around \code{\link[=gmm_conditionalise]{gmm_conditionalise()}} for the common case
where the observed coordinates are specified by integer index rather
than \code{NA}-padded vector. Equivalent to \code{\link[=gmm_observe]{gmm_observe()}} with a
selection matrix \code{A} and zero noise covariance, but routed through
the Schur-complement path for efficiency.
}
\examples{
g <- gmm(weights = c(0.4, 0.6),
         means = list(c(-1, 0), c(1, 0)),
         covariances = list(diag(2), diag(2)))
# Condition coord 2 on the value 0.5; keep coord 1.
gmm_missing(g, observed = 2L, values = 0.5)
}
\seealso{
Other operators: 
\code{\link{gmm_affine}()},
\code{\link{gmm_aggregate}()},
\code{\link{gmm_counterfactual}()},
\code{\link{gmm_filter}()},
\code{\link{gmm_intervene}()},
\code{\link{gmm_observe}()},
\code{\link{gmm_reduce}()}
}
\concept{operators}
