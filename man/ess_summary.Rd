% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{ess_summary}
\alias{ess_summary}
\title{Summary of importance-sampling diagnostics}
\usage{
ess_summary(fit)
}
\arguments{
\item{fit}{A \link{gmm_fit}.}
}
\value{
A list of numeric scalars (or \code{NA}s where not applicable), plus a
logical \code{support_truncated} flag that is \code{TRUE} when the fit's auto-selected
proposal replaced an unbounded target-support end with a finite,
data-derived working box (so \code{support_fraction} reflects only that finite
region and the target tail beyond it was not sampled). The finite box
itself is recorded in \code{fit@diagnostics$working_bounds}.
}
\description{
Convenience accessor returning the headline IS-quality numbers for a
regime-(iii) fit: effective sample size and its ratio to \code{is_size},
the largest self-normalised weight, the support fraction (proportion
of draws that received a finite weight), and the Monte-Carlo standard
error of the final KLD estimate. Returns \code{NA} fields for regimes that
do not use importance sampling.
}
\details{
Validation-side numbers (\verb{validation_*}) are populated only when the
fit was called with \code{validation_size > 0}.
}
\examples{
fit <- fit_proxymix(banana_target(), N = 3L, regime = "kld",
                    is_size = 1500L, max_iter = 20L, seed = 1L,
                    validation_size = 1500L)
ess_summary(fit)
}
\seealso{
Other diagnostics: 
\code{\link{bic_aic}()},
\code{\link{ess_trace}()},
\code{\link{gmm_anneal_path}()},
\code{\link{gmm_conditional_entropy}()},
\code{\link{gmm_entropy}()},
\code{\link{gmm_evidence}()},
\code{\link{gmm_fit_ensemble}()},
\code{\link{gmm_fit_quality}()},
\code{\link{gmm_independence_graph}()},
\code{\link{gmm_mutual_information}()},
\code{\link{hellinger_mc}()},
\code{\link{kld_trace}()},
\code{\link{proxy_functional_ci}()}
}
\concept{diagnostics}
