% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/impute_sensitivity.R
\name{proxy_mnar_sensitivity}
\alias{proxy_mnar_sensitivity}
\title{Missing-not-at-random sensitivity analysis for a coordinate mean}
\usage{
proxy_mnar_sensitivity(
  data,
  coord,
  beta_grid = seq(0, 1, by = 0.25),
  link = c("logit", "probit"),
  N = NULL,
  m = 20L,
  seed = NULL,
  ...
)
}
\arguments{
\item{data}{A numeric matrix or data frame with \code{NA} in \code{coord} only (its
other columns must be fully observed).}

\item{coord}{Name or index of the coordinate the mechanism acts on.}

\item{beta_grid}{Numeric vector of sensitivity slopes. Positive values make
larger unobserved values more likely to be missing.}

\item{link}{Selection link, \code{"logit"} (the default) or \code{"probit"}.}

\item{N, m, seed, ...}{Passed to \code{\link[=gmm_impute]{gmm_impute()}}; a single \code{seed} makes the whole
sweep reproducible and keeps the curve smooth across the grid.}
}
\value{
A data frame with one row per grid value: \code{beta}, \code{estimate},
\code{std.error}, \code{conf.low}, \code{conf.high}, \code{fmi}.
}
\description{
Sweeps the missing-not-at-random sensitivity slope \code{beta} over a grid and, at
each value, multiply-imputes \code{coord} under the selection model
\eqn{P(\text{missing}\mid y) = g(\alpha + \beta y)} and pools its mean by
Rubin's rules. The result traces how the estimate and its confidence interval
move as the assumed dependence of missingness on the unobserved value
strengthens, so an analyst can read off the value of \code{beta} at which a
conclusion would change. \code{beta = 0} is the missing-at-random anchor.
}
\details{
The slope is a sensitivity parameter, not an estimate: the data do not identify
it. Report the curve, not a single point.
}
\examples{
set.seed(1)
x1 <- rnorm(300)
y <- x1 + rnorm(300)
y[runif(300) < plogis(-0.4 + 0.8 * y)] <- NA      # MNAR on y
dat <- data.frame(x1 = x1, y = y)
proxy_mnar_sensitivity(dat, "y", beta_grid = c(0, 0.4, 0.8, 1.2), m = 10L, seed = 1L)
}
\seealso{
\code{\link[=gmm_impute]{gmm_impute()}}, \code{\link[=mnar]{mnar()}}.

Other imputation: 
\code{\link{as_mids}()},
\code{\link{gmm_complete}()},
\code{\link{gmm_imputation}()},
\code{\link{gmm_impute}()},
\code{\link{mechanism}},
\code{\link{proxy_fmi}()},
\code{\link{proxy_pool}()}
}
\concept{imputation}
