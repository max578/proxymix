% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/entropy.R
\name{gmm_divergence}
\alias{gmm_divergence}
\title{Divergence between two Gaussian mixtures}
\usage{
gmm_divergence(p, q, type = c("cs", "kl"), n_mc = 5000L)
}
\arguments{
\item{p, q}{Two \link{gmm} (or \link{gmm_fit}) objects of the same ambient dimension.}

\item{type}{\code{"cs"} (closed-form symmetric Cauchy-Schwarz divergence, the
default) or \code{"kl"} (delegates to \code{\link[=gmm_kld]{gmm_kld()}}).}

\item{n_mc}{Number of Monte Carlo samples used when \code{type = "kl"}.}
}
\value{
For \code{type = "cs"}, a non-negative numeric scalar. For \code{type = "kl"},
the list returned by \code{\link[=gmm_kld]{gmm_kld()}}.
}
\description{
Computes a divergence between two Gaussian mixtures of the same ambient
dimension. The Cauchy-Schwarz divergence
\deqn{D_{\mathrm{CS}}(p, q) = \tfrac{1}{2}\log V(p, p)
  + \tfrac{1}{2}\log V(q, q) - \log V(p, q),}
with \eqn{V(p, q) = \int p(x) q(x)\, dx}, is closed-form, symmetric,
non-negative, and zero exactly when \eqn{p \propto q}. The \code{"kl"} option
delegates to \code{\link[=gmm_kld]{gmm_kld()}}, a Monte-Carlo estimate of the asymmetric
Kullback-Leibler divergence \eqn{\mathrm{KL}(p \Vert q)}.
}
\examples{
p <- gmm(weights = c(0.5, 0.5),
         means = list(c(-1, 0), c(1, 0)),
         covariances = list(diag(2), diag(2)))
q <- gmm(weights = 1, means = list(c(0, 0)),
         covariances = list(diag(2) * 2))
gmm_divergence(p, q)
gmm_divergence(p, p)
}
\seealso{
\code{\link[=gmm_entropy]{gmm_entropy()}}, \code{\link[=gmm_kld]{gmm_kld()}}

Other ops: 
\code{\link{dgmm}()},
\code{\link{gmm_canonicalise}()},
\code{\link{gmm_conditionalise}()},
\code{\link{gmm_kld}()},
\code{\link{gmm_marginalise}()},
\code{\link{gmm_modes}()},
\code{\link{rgmm}()}
}
\concept{ops}
