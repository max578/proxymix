% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/impute_pool.R
\name{proxy_pool}
\alias{proxy_pool}
\title{Pool a column mean across imputations}
\usage{
proxy_pool(object, column, method = c("analytic", "rubin"))
}
\arguments{
\item{object}{A \link{gmm_imputation}.}

\item{column}{Name of a single numeric column whose mean is pooled.}

\item{method}{\code{"analytic"} (the default) for the closed-form pooling, or
\code{"rubin"} for Rubin's rules over the drawn completions.}
}
\value{
A one-row data frame: \code{term}, \code{estimate}, \code{std.error},
\code{statistic}, \code{df}, \code{conf.low}, \code{conf.high}, \code{fmi}.
}
\description{
Pools the mean of one column over the \code{m} completed datasets in a
\link{gmm_imputation}, returning the estimate with a standard error, degrees of
freedom, confidence interval, and fraction of missing information. The
default \code{method = "analytic"} computes the between-imputation variance in
closed form -- the exact \eqn{m \to \infty} limit, with no Monte-Carlo
noise -- from the mixture conditional, and splits the total variance into
complete-data, imputation, and parameter parts. \code{method = "rubin"} instead
applies Rubin's rules to the drawn completions (useful as a check).
}
\details{
For a regression or any other model estimand, do not pool here: convert the
imputations to a \code{mice} object with \code{\link[=as_mids]{as_mids()}} and pool with
\code{\link[mice:pool]{mice::pool()}}, which is the established workflow and reports the same
diagnostics.
}
\examples{
set.seed(1)
x1 <- rnorm(150); x2 <- x1 + rnorm(150)
x2[runif(150) < 0.3] <- NA
imp <- gmm_impute(cbind(x1, x2), N = 1L, m = 10L, seed = 1L)
proxy_pool(imp, "x2")                       # analytic column mean
}
\seealso{
\code{\link[=proxy_fmi]{proxy_fmi()}}, \code{\link[=as_mids]{as_mids()}} to pool models with \code{\link[mice:pool]{mice::pool()}}.

Other imputation: 
\code{\link{as_mids}()},
\code{\link{gmm_complete}()},
\code{\link{gmm_imputation}()},
\code{\link{gmm_impute}()},
\code{\link{mechanism}},
\code{\link{proxy_fmi}()},
\code{\link{proxy_mnar_sensitivity}()}
}
\concept{imputation}
