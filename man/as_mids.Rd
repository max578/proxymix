% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/impute_mice.R
\name{as_mids}
\alias{as_mids}
\title{Convert imputations to a mice multiply-imputed dataset}
\usage{
as_mids(object)
}
\arguments{
\item{object}{A \link{gmm_imputation}.}
}
\value{
A \code{mice::mids} object with \code{m} imputations.
}
\description{
Packages a \link{gmm_imputation} as a \code{mice::mids} object so that an arbitrary
model estimand can be fitted and pooled with the established mice workflow.
The joint Gaussian-mixture imputations -- including the multimodal and
heteroscedastic shapes a univariate imputer cannot produce -- flow through
unchanged; mice supplies \code{with()}, \code{\link[mice:pool]{mice::pool()}}, and the pooled
diagnostics.
}
\examples{
set.seed(1)
x1 <- rnorm(150); x2 <- x1 + rnorm(150)
x2[runif(150) < 0.3] <- NA
imp <- gmm_impute(cbind(x1, x2), N = 1L, m = 10L, seed = 1L)
if (requireNamespace("mice", quietly = TRUE)) {
  fit <- with(as_mids(imp), lm(x2 ~ x1))
  summary(mice::pool(fit))
}
}
\seealso{
\code{\link[=gmm_impute]{gmm_impute()}}, \code{\link[=proxy_pool]{proxy_pool()}} for the closed-form column-mean
pooling.

Other imputation: 
\code{\link{gmm_complete}()},
\code{\link{gmm_imputation}()},
\code{\link{gmm_impute}()},
\code{\link{proxy_fmi}()},
\code{\link{proxy_pool}()}
}
\concept{imputation}
