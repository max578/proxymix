% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/end-of-sample.R
\name{gmm_eos_test}
\alias{gmm_eos_test}
\title{End-of-sample instability test on a Gaussian state-space filter}
\usage{
gmm_eos_test(
  prior,
  dynamics,
  measurement,
  y,
  m = 1L,
  method = c("chisq", "andrews"),
  alpha = 0.05
)
}
\arguments{
\item{prior}{A single-component \link{gmm} giving the state prior (the test is
defined for a fitted linear-Gaussian model; multi-component priors are not
yet supported).}

\item{dynamics}{A list with \code{A} (the state-transition matrix), \code{Q} (the
process-noise covariance) and an optional offset \code{b}, or a function
\verb{function(t)} returning such a list, exactly as for \code{\link[=gmm_filter]{gmm_filter()}}.
Gaussian-sum (mixture) process noise is rejected: the calibrations
below are defined for Gaussian innovations only.}

\item{measurement}{A list with \code{C} (the observation matrix), \code{R} (the
observation-noise covariance) and an optional offset \code{d}, or a function
\verb{function(t)} returning such a list, exactly as for \code{\link[=gmm_filter]{gmm_filter()}}.
Gaussian-sum measurement noise is rejected for the same reason.}

\item{y}{A numeric vector or an \verb{n x d} matrix of observations.}

\item{m}{Integer; the number of end-of-sample observations to test, \verb{1 <= m < nrow(y)}. The tiny-\code{m} regime (\verb{m = 1, 2, 3}) is the point of the test.}

\item{method}{Either \code{"chisq"} (parametric) or \code{"andrews"} (distribution-free
subsampling P-test).}

\item{alpha}{The nominal level used to set \code{reject}.}
}
\value{
An object of class \code{gmm_eos_test}: a list with the \code{statistic}, the
\code{p_value}, the logical \code{reject}, the \code{method}, \code{m}, \code{alpha}, and the
in-sample block statistics used for the subsampling calibration.
}
\description{
Tests whether the last \code{m} observations of a series are consistent with a
linear-Gaussian state-space model fitted on the rest, in the regime where \code{m}
is small (even \code{m = 1}) and smaller than the parameter count -- where ordinary
structural-break tests (Chow, sup-Wald) are undefined because the post-break
parameters cannot be estimated. The statistic is the sum of the last \code{m}
squared standardised one-step innovations from the filter; a break inflates it.
}
\details{
Two calibrations are offered. \code{method = "chisq"} refers the statistic to a
chi-square distribution on \code{m * ncol(y)} degrees of freedom, which is exact
when the standardised innovations are Gaussian. \code{method = "andrews"} is the
distribution-free Andrews (2003) subsampling P-test: the statistic's rank among
the in-sample overlapping \code{m}-blocks of the same innovations gives the p-value,
so it stays calibrated when the innovations are non-Gaussian (heavy-tailed
observation noise, say). The model is supplied exactly as for \code{\link[=gmm_filter]{gmm_filter()}}.
}
\examples{
prior <- gmm(weights = 1, means = list(0), covariances = list(matrix(10)))
dyn   <- list(A = matrix(1), Q = matrix(0.04))
meas  <- list(C = matrix(1), R = matrix(1))
set.seed(1)
y <- c(rnorm(119), 6)                # a stable series with a jump at the end
gmm_eos_test(prior, dyn, meas, y, m = 1L, method = "andrews")
}
\references{
Andrews, D. W. K. (2003). End-of-Sample Instability Tests.
\emph{Econometrica}, 71(6), 1661--1694.
}
\seealso{
\code{\link[=gmm_filter]{gmm_filter()}}
}
