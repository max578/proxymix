% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/impute_mechanism.R
\name{mechanism}
\alias{mechanism}
\alias{mar}
\alias{mnar}
\alias{censored}
\title{Missingness mechanisms for multiple imputation}
\usage{
mar()

mnar(coord, beta, link = c("logit", "probit"))

censored(coord, lower = -Inf, upper = Inf)
}
\arguments{
\item{coord}{Name or index of the single coordinate the mechanism acts on.}

\item{beta}{Sensitivity slope of the log-odds (or probit score) of being
missing in the missing value itself. Positive \code{beta} makes larger values
more likely to be missing.}

\item{link}{Selection link, \code{"logit"} (the default) or \code{"probit"}.}

\item{lower, upper}{Bounds of the interval a censored missing entry is known to
lie in. At least one must be finite.}
}
\value{
A \code{proxymix_gate} object for \code{\link[=gmm_impute]{gmm_impute()}}.
}
\description{
Specifications passed to the \code{mechanism} argument of \code{\link[=gmm_impute]{gmm_impute()}}. They
describe how an entry came to be missing, which sets the conditional the
missing value is drawn from.
}
\details{
\code{mar()} is missing at random: the probability that an entry is missing may
depend on the observed entries but not on the missing value, so the imputation
conditional is the plain mixture conditional. This is the default.

\code{censored()} is a known interval. A missing entry of \code{coord} is known only to
lie in \verb{[lower, upper]} -- a detection limit (\code{upper = LOD}), a ceiling
(\code{lower = cap}), or interval censoring. The imputation conditional is the
mixture conditional truncated to that interval, which proxymix evaluates in
closed form, so the imputations respect the bound instead of substituting a
constant such as half the detection limit.

\code{mnar()} is missing not at random through a selection model: an entry of
\code{coord} is missing with probability \eqn{g(\alpha + \beta\, y)} in its own
unobserved value \eqn{y}, where \eqn{g} is the logistic or normal link. The
slope \code{beta} is the sensitivity parameter and is supplied, not estimated --
missing-not-at-random departures are not identified from the observed data, so
the appropriate use is to posit \code{beta}, propagate it, and report how conclusions
move with it (see \code{\link[=proxy_mnar_sensitivity]{proxy_mnar_sensitivity()}}). The intercept is calibrated to
the observed missingness rate. \code{beta = 0} is missing at random.
}
\examples{
mar()
censored("y", upper = 0.5)             # a lower detection limit at 0.5
mnar("y", beta = 0.8)                  # larger y more likely missing
}
\seealso{
\code{\link[=gmm_impute]{gmm_impute()}}, \code{\link[=proxy_mnar_sensitivity]{proxy_mnar_sensitivity()}}.

Other imputation: 
\code{\link{as_mids}()},
\code{\link{gmm_complete}()},
\code{\link{gmm_imputation}()},
\code{\link{gmm_impute}()},
\code{\link{proxy_fmi}()},
\code{\link{proxy_mnar_sensitivity}()},
\code{\link{proxy_pool}()}
}
\concept{imputation}
