% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/operator_calculus.R
\name{gmm_affine}
\alias{gmm_affine}
\title{Affine pushforward of a Gaussian mixture}
\usage{
gmm_affine(g, A, b = 0, noise_cov = NULL, ridge_eps = 1e-06)
}
\arguments{
\item{g}{A \link{gmm} (or \link{gmm_fit}) in \code{R^p}.}

\item{A}{An \code{m} by \code{p} numeric matrix.}

\item{b}{Numeric scalar or length-\code{m} vector. Default \code{0}.}

\item{noise_cov}{\code{m} by \code{m} SPD numeric matrix, or \code{NULL} (treated as
the zero matrix — a deterministic channel).}

\item{ridge_eps}{Tiny ridge added to the output covariances for
numerical hygiene. Set to \code{0} to disable.}
}
\value{
A \link{gmm} in \code{R^m} with the same number of components and the
same weights as \code{g}.
}
\description{
Returns the (closed-form) distribution of \eqn{Y = A X + b + \epsilon}
when \eqn{X \sim g} is a Gaussian mixture and \eqn{\epsilon \sim
\mathcal{N}(0, R)} is independent additive Gaussian noise.
}
\details{
For each component \code{k}, the pushed-forward parameters are
\deqn{\mu'_k = A \mu_k + b, \qquad \Sigma'_k = A \Sigma_k A^\top + R,}
and the mixture weights are unchanged. This is the finite-mixture
analogue of a Kalman-style predict step.

The channel is required to be \strong{affine} in \code{x} and the noise is
required to be Gaussian. Non-linear channels are not closed form and
must not be silently approximated; pass them to a Monte Carlo helper
instead.
}
\examples{
g <- gmm(weights = c(0.5, 0.5),
         means = list(c(-1, 0), c(1, 0)),
         covariances = list(diag(2), diag(2)))
A <- matrix(c(1, 0, 0, 1, 1, -1), nrow = 3L, byrow = TRUE)
gmm_affine(g, A, b = c(0, 0, 0), noise_cov = 0.01 * diag(3))
}
\seealso{
Other operators: 
\code{\link{gmm_aggregate}()},
\code{\link{gmm_missing}()},
\code{\link{gmm_observe}()}

Other v0_3: 
\code{\link{gmm_aggregate}()},
\code{\link{gmm_missing}()},
\code{\link{gmm_observe}()}
}
\concept{operators}
\concept{v0_3}
