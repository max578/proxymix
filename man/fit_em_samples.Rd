% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_em_samples.R
\name{fit_em_samples}
\alias{fit_em_samples}
\title{Classical EM fit on samples}
\usage{
fit_em_samples(
  target,
  N = 2L,
  init = NULL,
  max_iter = 100L,
  tol = 1e-06,
  ridge_eps = 1e-06,
  n_starts = 5L,
  canonicalise = TRUE
)
}
\arguments{
\item{target}{A \link{gmm_target} carrying an \code{n} by \code{p} \code{samples} matrix.}

\item{N}{Number of mixture components.}

\item{init}{A \link{gmm} initialisation, or \code{NULL} to use \code{\link[=init_kmeans]{init_kmeans()}}.}

\item{max_iter}{Maximum number of EM iterations.}

\item{tol}{Relative-log-likelihood convergence tolerance.}

\item{ridge_eps}{Ridge added to each component covariance at every M-step.}

\item{n_starts}{Number of multi-start initialisations (only when \code{init}
is \code{NULL}). The best fit by final log-likelihood is returned.}

\item{canonicalise}{Logical. If \code{TRUE} (the default), the fitted
mixture is post-processed by \code{\link[=gmm_canonicalise]{gmm_canonicalise()}} so that components
are sorted by descending weight and (as a tiebreaker) by descending
\verb{||mu||}.}
}
\value{
A \link{gmm_fit} with \code{regime = "sample"}.
}
\description{
Implements regime (ii) of Hoek and Elliott (2024). Runs the textbook
expectation-maximisation algorithm for Gaussian mixtures on the supplied
samples, with diagonal ridge regularisation for numerical stability,
optional multi-start, and monotone-log-likelihood checking.
}
\examples{
x <- matrix(stats::rnorm(200), ncol = 2)
tgt <- gmm_target_from_samples(x)
fit <- fit_em_samples(tgt, N = 2L, max_iter = 30L, n_starts = 2L)
fit@diagnostics$loglik_final
}
\seealso{
Other fitting: 
\code{\link{fit_kld_em}()},
\code{\link{fit_moment_match}()},
\code{\link{from_fb_posterior}()},
\code{\link{from_kde}()}
}
\concept{fitting}
