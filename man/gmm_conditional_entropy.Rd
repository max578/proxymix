% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/entropy.R
\name{gmm_conditional_entropy}
\alias{gmm_conditional_entropy}
\title{Conditional predictive entropy of a Gaussian mixture}
\usage{
gmm_conditional_entropy(
  g,
  given,
  order = c("renyi2", "shannon"),
  n_mc = 5000L,
  seed = NULL
)
}
\arguments{
\item{g}{A \link{gmm} (or \link{gmm_fit}) joint mixture.}

\item{given}{Either a numeric vector with one entry per coordinate, or a
matrix whose rows are such vectors. \code{NA} marks a target (kept) coordinate;
a numeric value conditions on that coordinate (the \code{\link[=gmm_conditionalise]{gmm_conditionalise()}}
convention).}

\item{order}{\code{"renyi2"} (closed-form, the default) or \code{"shannon"}.}

\item{n_mc, seed}{Passed to \code{\link[=gmm_entropy]{gmm_entropy()}} for \code{order = "shannon"}.}
}
\value{
A numeric scalar for a single configuration, or a numeric vector
with one entropy per row of \code{given}.
}
\description{
Returns the differential entropy of the conditional mixture \eqn{g_{Y \mid X
= x}} obtained from \code{\link[=gmm_conditionalise]{gmm_conditionalise()}} -- the predictive uncertainty of
the target coordinates given the conditioned ones. The order-2 Renyi entropy
is closed-form; \code{order = "shannon"} falls back to Monte Carlo. Multiple
conditioning configurations are evaluated row-by-row.
}
\examples{
## Joint over (Y, X); predictive entropy of Y at several X values.
s <- matrix(c(2, 0.8, 0.8, 1), 2, 2)
g <- gmm(weights = 1, means = list(c(0, 0)), covariances = list(s))
gmm_conditional_entropy(g, given = rbind(c(NA, 0), c(NA, 1)))
}
\seealso{
\code{\link[=gmm_entropy]{gmm_entropy()}}, \code{\link[=gmm_conditionalise]{gmm_conditionalise()}}

Other diagnostics: 
\code{\link{bic_aic}()},
\code{\link{ess_summary}()},
\code{\link{ess_trace}()},
\code{\link{gmm_anneal_path}()},
\code{\link{gmm_entropy}()},
\code{\link{gmm_mutual_information}()},
\code{\link{hellinger_mc}()},
\code{\link{kld_trace}()}
}
\concept{diagnostics}
